---
phase: 05-spec-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/ARCHITECTURE.md
  - spec/DATA_LAYER.md
  - spec/UI_SPEC.md
  - spec/CONSTANTS.md
autonomous: true
requirements: [BUG-05]

must_haves:
  truths:
    - "ThrottleTracker is documented in spec/DATA_LAYER.md with its struct definition, methods, and purpose"
    - "AccountStore.canAddAccount is documented as '< maxAccounts' (i.e. < 3), not '< 2'"
    - "All Phase 1-4 code changes are reflected in spec files with no remaining drift"
    - "spec/ARCHITECTURE.md project tree includes ThrottleTracker.swift, ThrottleTrackerTests.swift, and SessionLogReaderDiscoveryTests.swift"
  artifacts:
    - path: "spec/DATA_LAYER.md"
      provides: "Updated service and model documentation"
      contains: "ThrottleTracker"
    - path: "spec/ARCHITECTURE.md"
      provides: "Updated project tree"
      contains: "ThrottleTracker.swift"
    - path: "spec/CONSTANTS.md"
      provides: "Accurate constant values"
    - path: "spec/UI_SPEC.md"
      provides: "Accurate UI descriptions"
  key_links:
    - from: "spec/DATA_LAYER.md"
      to: "AIBattery/Utilities/ThrottleTracker.swift"
      via: "spec documents the struct"
      pattern: "ThrottleTracker"
    - from: "spec/DATA_LAYER.md"
      to: "AIBattery/Services/AccountStore.swift"
      via: "spec matches code signature"
      pattern: "canAddAccount.*< maxAccounts"
---

<objective>
Bring all 4 spec files current with the codebase after Phases 1-4. Fix known drift (ThrottleTracker undocumented, AccountStore.canAddAccount wrong) and audit for all other divergences introduced by bidirectional tier detection, dynamic probe models, JSONL tool call counting, adaptive polling changes, and discovery TTL.

Purpose: Spec files are the source of truth per CLAUDE.md. Stale specs cause incorrect implementations in future work.
Output: Updated spec/ARCHITECTURE.md, spec/DATA_LAYER.md, spec/UI_SPEC.md, spec/CONSTANTS.md
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-spec-sync/05-CONTEXT.md
@.planning/phases/01-context-projection-fixes/01-01-SUMMARY.md
@.planning/phases/02-data-accuracy-fixes/02-01-SUMMARY.md
@.planning/phases/02-data-accuracy-fixes/02-02-SUMMARY.md
@.planning/phases/04-polling-discovery-hardening/04-01-SUMMARY.md
@.planning/phases/04-polling-discovery-hardening/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit all spec files against source code for drift</name>
  <files>spec/ARCHITECTURE.md, spec/DATA_LAYER.md, spec/UI_SPEC.md, spec/CONSTANTS.md</files>
  <read_first>
    spec/ARCHITECTURE.md
    spec/DATA_LAYER.md
    spec/UI_SPEC.md
    spec/CONSTANTS.md
    AIBattery/Utilities/ThrottleTracker.swift
    AIBattery/Services/AccountStore.swift
    AIBattery/Services/RateLimitFetcher.swift
    AIBattery/Services/TokenHealthMonitor.swift
    AIBattery/Services/SessionLogReader.swift
    AIBattery/Services/UsageAggregator.swift
    AIBattery/Models/SessionEntry.swift
    AIBattery/ViewModels/UsageViewModel.swift
    AIBattery/Services/FileWatcher.swift
    Tests/AIBatteryCoreTests/Utilities/ThrottleTrackerTests.swift
    Tests/AIBatteryCoreTests/Services/SessionLogReaderDiscoveryTests.swift
  </read_first>
  <action>
Read every source file listed above and compare against the corresponding spec sections. Build a comprehensive drift list. The following drift items are already known — confirm each and find any additional ones:

**ARCHITECTURE.md drift:**
- Project tree missing: `ThrottleTracker.swift` (under Utilities/), `ThrottleTrackerTests.swift` (under Tests/Utilities/), `SessionLogReaderDiscoveryTests.swift` (under Tests/Services/)

**DATA_LAYER.md drift:**
- AccountStore: `canAddAccount` documented as `(< 2)` on the Computed line but code uses `< Self.maxAccounts` which is 3. Fix to `(< maxAccounts)`.
- ThrottleTracker: Entirely missing. Needs a new section under Utilities (or between Services, since it's in Utilities/).
- SessionEntry/SessionMessage: Missing `ContentBlock` struct (type-only decoding), missing `content: [ContentBlock]?` field on `SessionMessage`, missing `toolCallCount: Int` on `AssistantUsageEntry`.
- RateLimitFetcher: Still describes hardcoded 3-model fallback list. Needs update to document: `observedModels`, `ultimateFallback`, `setObservedModels(_:accountId:)`, `restoreWorkingModels()`, `saveWorkingModel` called on all 4 success paths (200-OK, 429+headers, retry-after, 400+headers), UserDefaults persistence under `aibattery_observedModels_{accountId}`.
- TokenHealthMonitor: Missing bidirectional (downward) tier adjustment. The spec only mentions upward adjustment. Add: downward check after upward, anti-thrash guard (only downgrades when observed < lower tier boundary).
- SessionLogReader: Missing `discoveryTTL` (60s static let), `lastFullEnumerationDate`, `expireDiscoveryTTLForTesting()`. Discovery caching description needs update to mention TTL as additional condition for cache hit.
- UsageAggregator: Line says "Tool calls from stats cache only" — now uses `max(jsonlTodayToolCalls, statsCacheToolCalls)`. Also missing `lastSeenByModel` tracking that feeds `RateLimitFetcher.setObservedModels`.
- UsageSnapshot: `todayToolCalls` source described as "stats-cache dailyActivity for today" — now merged from both sources.

**CONSTANTS.md drift:**
- Verify Discovery TTL (60 sec) is already present (was added in Phase 04-02 commit). Confirm no other missing constants from Phase 1-4 changes.
- Check if `aibattery_observedModels_{accountId}` UserDefaults key is documented.
- Check if `aibattery_throttleTimestamps_{accountId}` UserDefaults key is documented.

**UI_SPEC.md drift:**
- Check if the `throttleCount(days:)` reference is still accurate given ThrottleTracker extraction.
- Check for any other UI-facing changes from Phases 1-4.

Output a structured list of every drift item found, organized by spec file. This is a working document for Task 2 — no files are modified in this task.
  </action>
  <verify>
    <automated>echo "Task 1 is an audit — verify by checking that Task 2 addresses all items found"</automated>
  </verify>
  <acceptance_criteria>
    - Comprehensive drift list covers all 4 spec files
    - Every Phase 1-4 SUMMARY's code changes are checked against specs
    - Both known items (ThrottleTracker, canAddAccount) confirmed
    - Any additional drift items beyond the known list are identified
  </acceptance_criteria>
  <done>A complete, structured drift list exists covering ARCHITECTURE.md, DATA_LAYER.md, CONSTANTS.md, and UI_SPEC.md with every divergence identified</done>
</task>

<task type="auto">
  <name>Task 2: Update all 4 spec files to match current codebase</name>
  <files>spec/ARCHITECTURE.md, spec/DATA_LAYER.md, spec/UI_SPEC.md, spec/CONSTANTS.md</files>
  <read_first>
    spec/ARCHITECTURE.md
    spec/DATA_LAYER.md
    spec/UI_SPEC.md
    spec/CONSTANTS.md
  </read_first>
  <action>
Apply every fix from the Task 1 drift list. No production code changes — spec files only.

**ARCHITECTURE.md updates:**
1. Add `ThrottleTracker.swift — Pure value type tracking throttle event transitions for trend display` to the Utilities/ section of the project tree (after `DurationFormatter.swift`, alphabetical order).
2. Add `ThrottleTrackerTests.swift — Throttle transition detection, timestamp parsing, pruning, counting` to the Tests/Utilities/ section of the project tree.
3. Add `SessionLogReaderDiscoveryTests.swift — TTL-based discovery fallback, cache expiry` to the Tests/Services/ section.

**DATA_LAYER.md updates:**
1. Fix AccountStore `canAddAccount` computed property description from `(< 2)` to `(< maxAccounts)`.
2. Add a new `### ThrottleTracker (Utilities/ThrottleTracker.swift)` section. Place it after TokenHealthMonitor or in a Utilities subsection. Document:
   - Pure value type (struct), immutable pattern (evaluate returns new tracker)
   - `wasThrottled: Bool` (private(set)) — tracks previous state
   - `evaluate(_ rateLimits: RateLimitUsage?) -> (tracker: ThrottleTracker, recordTimestamp: Double?)` — detects normal-to-throttled/exhausted transition, returns new tracker + optional timestamp to record
   - `static parseTimestamps(_ raw: [Any]?) -> [Double]` — handles Double, String, Int storage variants
   - `static appendAndPrune(timestamps:newTimestamp:) -> [Double]` — append + 30-day pruning
   - `static count(timestamps:days:) -> Int` — count within N days window
   - Used by UsageViewModel for throttle trend tracking (throttleCount(days:) in UI)
3. Update SessionEntry/SessionMessage section: add `ContentBlock` (nested struct, `type: String` only), `content: [ContentBlock]?` on SessionMessage, `toolCallCount: Int` on AssistantUsageEntry (computed as `content?.filter { $0.type == "tool_use" }.count ?? 0`).
4. Update RateLimitFetcher section:
   - Replace hardcoded 3-model fallback description with: probe order is `activeUserModel` -> `lastWorkingModel[accountId]` -> `observedModels` -> `ultimateFallback` (single newest Sonnet).
   - Add: `observedModels: [String]` — dynamically populated from JSONL-observed models (most recent first), persisted to UserDefaults under `aibattery_observedModels_{accountId}`.
   - Add: `setObservedModels(_:accountId:)`, `restoreWorkingModels()`.
   - Add: `saveWorkingModel` called on all 4 success paths in `tryFetch` (200-OK, 429+headers, retry-after, 400+headers) — structural invariant.
5. Update TokenHealthMonitor section: add bidirectional tier adjustment description. After the existing upward adjustment note, add: "Also adjusts downward — if observed tokens fall below the next-lower tier boundary, downgrades to the smallest tier that fits. Anti-thrash guard: only downgrades when observed < lower tier boundary (e.g. 600K on a 1M window stays at 1M because 600K >= 500K)."
6. Update SessionLogReader section: add `discoveryTTL: TimeInterval = 60` (static let), `lastFullEnumerationDate` tracking, `expireDiscoveryTTLForTesting()` test hook. Update discovery caching description: cache hit requires BOTH unchanged directory mod-dates AND TTL not expired.
7. Update UsageAggregator section: replace "Tool calls from stats cache only" with "Tool calls merged: `max(jsonlTodayToolCalls, statsCacheToolCalls)` — JSONL counts tool_use content blocks from today's entries, stats-cache provides its own count, max() prevents either from underreporting." Add: `lastSeenByModel: [String: Date]` tracked in single-pass loop, sorted by recency, fed to `RateLimitFetcher.setObservedModels` when accountId available.
8. Update UsageSnapshot `todayToolCalls` source from "stats-cache dailyActivity for today" to "max(JSONL tool_use count, stats-cache dailyActivity) for today".
9. Update FileWatcher section if needed: clarify that FileWatcher does NOT reset adaptive polling counter (only invalidates caches and triggers refresh).

**CONSTANTS.md updates:**
- Add `aibattery_observedModels_{accountId}` to a relevant section if not present.
- Add `aibattery_throttleTimestamps_{accountId}` if not present.
- Verify Discovery TTL row exists (should already be there from Phase 04-02).
- Add any other missing constants found in Task 1.

**UI_SPEC.md updates:**
- Apply any drift items found in Task 1 for UI spec.
- Verify `throttleCount(days:)` reference is accurate.

**CRITICAL:** Do NOT modify any production Swift code. Only `.md` files in `spec/` are touched.
  </action>
  <verify>
    <automated>grep -c "ThrottleTracker" spec/DATA_LAYER.md && grep -c "< maxAccounts" spec/DATA_LAYER.md && grep -c "ThrottleTracker" spec/ARCHITECTURE.md && grep -c "observedModels" spec/DATA_LAYER.md && grep -c "discoveryTTL" spec/DATA_LAYER.md && grep -c "downward" spec/DATA_LAYER.md && grep -c "SessionLogReaderDiscoveryTests" spec/ARCHITECTURE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "ThrottleTracker" spec/DATA_LAYER.md returns 1+ matches
    - grep "< maxAccounts" spec/DATA_LAYER.md returns 1+ matches (canAddAccount fixed)
    - grep "ThrottleTracker.swift" spec/ARCHITECTURE.md returns 1+ matches (project tree)
    - grep "ThrottleTrackerTests" spec/ARCHITECTURE.md returns 1+ matches (project tree)
    - grep "SessionLogReaderDiscoveryTests" spec/ARCHITECTURE.md returns 1+ matches (project tree)
    - grep "observedModels" spec/DATA_LAYER.md returns 1+ matches (dynamic probe)
    - grep "discoveryTTL" spec/DATA_LAYER.md returns 1+ matches (TTL fallback)
    - grep "downward" spec/DATA_LAYER.md returns 1+ matches (bidirectional tier)
    - grep "Tool calls from stats cache only" spec/DATA_LAYER.md returns 0 matches (stale line removed)
    - No .swift files modified (git diff --name-only shows only spec/*.md)
  </acceptance_criteria>
  <done>All 4 spec files updated to match current codebase. ThrottleTracker documented. AccountStore.canAddAccount corrected. All Phase 1-4 changes reflected. Zero production code changes.</done>
</task>

</tasks>

<verification>
1. `grep -r "ThrottleTracker" spec/` returns matches in both ARCHITECTURE.md and DATA_LAYER.md
2. `grep "< 2" spec/DATA_LAYER.md` returns 0 matches for the canAddAccount line (was `< 2`, now `< maxAccounts`)
3. `grep "observedModels\|ultimateFallback\|saveWorkingModel" spec/DATA_LAYER.md` returns matches for dynamic probe documentation
4. `grep "discoveryTTL\|lastFullEnumeration" spec/DATA_LAYER.md` returns matches for TTL documentation
5. `grep "downward\|bidirectional" spec/DATA_LAYER.md` returns matches for tier adjustment documentation
6. `git diff --name-only` shows ONLY files in `spec/` directory (no production code)
7. `swift build` still succeeds (no code changes, but sanity check)
</verification>

<success_criteria>
- All known drift items from CONTEXT.md are fixed (ThrottleTracker documented, canAddAccount corrected)
- All Phase 1-4 code changes are reflected in specs (bidirectional tier, dynamic probes, tool call counting, adaptive polling fix, discovery TTL)
- Project tree in ARCHITECTURE.md is complete (no missing files)
- No production Swift code was modified
- BUG-05 requirement satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/05-spec-sync/05-01-SUMMARY.md`
</output>
