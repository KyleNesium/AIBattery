---
phase: 07-visual-polish
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - AIBattery/Views/TokenHealthSection.swift
  - AIBattery/Views/ProjectUsageSection.swift
  - AIBattery/Views/ActivityChartView.swift
  - AIBattery/Views/UsageBarsSection.swift
  - AIBattery/Views/UsagePopoverView.swift
  - AIBattery/Views/CollapsibleSectionHeader.swift
autonomous: true
requirements: [UI-07]

must_haves:
  truths:
    - "Expanding or collapsing any section plays a smooth opacity fade (no abrupt content jump)"
    - "Numeric values (percentages, token counts, costs) animate digit-by-digit when they change"
    - "All animation durations reference MotionConstants instead of inline literals"
    - "Animations do not fire when the popover panel is closed (view lifecycle gates them)"
  artifacts:
    - path: "AIBattery/Views/TokenHealthSection.swift"
      provides: "Opacity transition on collapsed content, contentTransition on health percentage"
      contains: ".transition(.opacity)"
    - path: "AIBattery/Views/ProjectUsageSection.swift"
      provides: "Opacity transition on collapsed content, contentTransition on cost/token text"
      contains: ".transition(.opacity)"
    - path: "AIBattery/Views/ActivityChartView.swift"
      provides: "Opacity transition on collapsed chart and insights content"
      contains: ".transition(.opacity)"
    - path: "AIBattery/Views/UsageBarsSection.swift"
      provides: "contentTransition on percentage text"
      contains: ".contentTransition(.numericText())"
    - path: "AIBattery/Views/CollapsibleSectionHeader.swift"
      provides: "MotionConstants.standard replacing inline 0.2 literal"
      contains: "MotionConstants.standard"
  key_links:
    - from: "AIBattery/Views/CollapsibleSectionHeader.swift"
      to: "AIBattery/Utilities/Spacing.swift"
      via: "MotionConstants.standard for withAnimation"
      pattern: "MotionConstants\\.standard"
    - from: "AIBattery/Views/TokenHealthSection.swift"
      to: "AIBattery/Utilities/Spacing.swift"
      via: "MotionConstants.snappy for gesture animations"
      pattern: "MotionConstants\\.snappy"
---

<objective>
Add smooth expand/collapse transitions and numeric value animations across all popover sections. Migrate inline animation duration literals to MotionConstants.

Purpose: UI-07 requires subtle transition animations on section expand/collapse and metric changes. This plan adds `.transition(.opacity)` to collapsible content, `.contentTransition(.numericText())` to numeric Text views, and replaces hardcoded animation durations with MotionConstants references.
Output: All collapsible sections fade smoothly, numeric values animate digit-by-digit, animation constants centralized.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-visual-polish/07-CONTEXT.md
@.planning/phases/07-visual-polish/07-RESEARCH.md
@.planning/phases/07-visual-polish/07-01-SUMMARY.md

<interfaces>
<!-- Created by Plan 01 — executor depends on these -->
From AIBattery/Utilities/Spacing.swift:
```swift
enum MotionConstants {
    static let standard: Animation = .easeInOut(duration: 0.2)
    static let snappy: Animation = .easeInOut(duration: 0.15)
}
```

From AIBattery/Views/CollapsibleSectionHeader.swift:
```swift
struct CollapsibleSectionHeader: View {
    let title: String
    @Binding var collapsed: Bool
    var tooltip: String = ""
    // Button action: withAnimation(.easeInOut(duration: 0.2)) { collapsed.toggle() }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add opacity transitions to collapsible sections and contentTransition to numeric Text views</name>
  <files>AIBattery/Views/TokenHealthSection.swift, AIBattery/Views/ProjectUsageSection.swift, AIBattery/Views/ActivityChartView.swift, AIBattery/Views/UsageBarsSection.swift</files>
  <read_first>AIBattery/Views/TokenHealthSection.swift, AIBattery/Views/ProjectUsageSection.swift, AIBattery/Views/ActivityChartView.swift, AIBattery/Views/UsageBarsSection.swift, AIBattery/Utilities/Spacing.swift</read_first>
  <action>
**A. Add `.transition(.opacity)` to collapsible content blocks:**

The `CollapsibleSectionHeader` already wraps `collapsed.toggle()` in `withAnimation(.easeInOut(duration: 0.2))`. Adding `.transition(.opacity)` to the content block inside `if !collapsed { ... }` makes content fade in/out with that existing animation driver.

1. **TokenHealthSection.swift** — Find the `if !collapsed {` block (around line 62) that wraps the main session content VStack. Add `.transition(.opacity)` to the VStack inside this block. There are multiple `if !collapsed` checks in this file (lines 52, 56, 62). The primary content block is at line 62 — that is the one to annotate. The others at lines 52 and 56 are small inline controls within the header area — leave those alone.

2. **ProjectUsageSection.swift** — Find the `if !collapsed && !snapshot.projectTokens.isEmpty {` block (around line 53). Add `.transition(.opacity)` to the content VStack inside it.

3. **ActivityChartView.swift** — Two content blocks:
   - `if !collapsed {` (around line 114) — chart content. Add `.transition(.opacity)` to the VStack.
   - `if !collapsed, let snapshot {` (around line 154) — insights content. Add `.transition(.opacity)` to the VStack.

**B. Add `.contentTransition(.numericText())` to numeric Text views:**

Use `contentTransition(.numericText())` (NO arguments — macOS 13+ safe). Do NOT use `numericText(value:)` which requires macOS 14.

1. **UsageBarsSection.swift**:
   - `Text("\(Int(percent))%")` (around line 75) — the usage bar percentage. Add `.contentTransition(.numericText())`.

2. **TokenHealthSection.swift**:
   - `Text("\(Int(health.usagePercentage))%")` (around line 297) — the health percentage badge. Add `.contentTransition(.numericText())`.
   - `Text("\(selectedIndex + 1)/\(sessions.count)")` (around line 193) — session counter. Add `.contentTransition(.numericText())`.

3. **ProjectUsageSection.swift**:
   - `Text(costText)` (around line 98) — project cost in header. Add `.contentTransition(.numericText())`.
   - `Text(totalTokensText)` (around line 103) — total tokens in header. Add `.contentTransition(.numericText())`.
   - `Text(costText)` (around line 169) — per-project cost column. Add `.contentTransition(.numericText())`.
   - `Text(tokensText)` (around line 174) — per-project tokens column. Add `.contentTransition(.numericText())`.

4. **ActivityChartView.swift**:
   - Numeric text in chart labels if any exist. Read the file and add `.contentTransition(.numericText())` to any `Text` view displaying numeric values that change on poll cycles (token counts, session counts). Skip static axis labels.

**C. Migrate inline animation durations in section files:**

Replace `withAnimation(.easeInOut(duration: 0.15))` with `withAnimation(MotionConstants.snappy)` in:
- **TokenHealthSection.swift** — 4 callsites (around lines 146, 167, 187, 201)

Replace `withAnimation(.easeInOut(duration: 0.2))` with `withAnimation(MotionConstants.standard)` in:
- **ProjectUsageSection.swift** — 1 callsite (around line 118)

DO NOT modify UsageBarsSection.swift's existing `.transition(.opacity)` (lines 115, 120) — these already work correctly.
DO NOT add `.contentTransition` to SettingsRow text (settings labels are not numeric data per anti-pattern guidance).
DO NOT add unscoped `.animation()` modifiers to container VStacks.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "\.transition(.opacity)" AIBattery/Views/TokenHealthSection.swift` returns at least 1
- `grep -c "\.transition(.opacity)" AIBattery/Views/ProjectUsageSection.swift` returns at least 1
- `grep -c "\.transition(.opacity)" AIBattery/Views/ActivityChartView.swift` returns at least 2
- `grep -c "contentTransition(.numericText())" AIBattery/Views/UsageBarsSection.swift` returns at least 1
- `grep -c "contentTransition(.numericText())" AIBattery/Views/TokenHealthSection.swift` returns at least 2
- `grep -c "contentTransition(.numericText())" AIBattery/Views/ProjectUsageSection.swift` returns at least 2
- `grep -c "MotionConstants.snappy" AIBattery/Views/TokenHealthSection.swift` returns 4
- `grep -c "MotionConstants.standard" AIBattery/Views/ProjectUsageSection.swift` returns 1
- `swift build` succeeds with zero errors
  </acceptance_criteria>
  <done>All collapsible sections have .transition(.opacity) on their content blocks. Numeric Text views have .contentTransition(.numericText()). Inline animation durations in section files replaced with MotionConstants references. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate remaining animation literals in CollapsibleSectionHeader and UsagePopoverView to MotionConstants</name>
  <files>AIBattery/Views/CollapsibleSectionHeader.swift, AIBattery/Views/UsagePopoverView.swift</files>
  <read_first>AIBattery/Views/CollapsibleSectionHeader.swift, AIBattery/Views/UsagePopoverView.swift, AIBattery/Utilities/Spacing.swift</read_first>
  <action>
**A. CollapsibleSectionHeader.swift:**
- Line 12: Replace `withAnimation(.easeInOut(duration: 0.2))` with `withAnimation(MotionConstants.standard)`

**B. UsagePopoverView.swift:**
Replace inline animation literals with MotionConstants references:

- `withAnimation(.easeInOut(duration: 0.2))` callsites → `withAnimation(MotionConstants.standard)`:
  - Settings toggle (around line 226): `withAnimation(.easeInOut(duration: 0.2)) { showSettings.toggle() }`
  - Any other 0.2-duration withAnimation calls in the file (around lines 327, 475)

- `.animation(.easeInOut(duration: 0.15), value: metricModeRaw)` (around line 130) → `.animation(MotionConstants.snappy, value: metricModeRaw)`

- `.animation(.easeInOut(duration: 0.15), value: showLogoutConfirm)` (around line 570) → `.animation(MotionConstants.snappy, value: showLogoutConfirm)`

DO NOT touch CopyableText.swift, MarqueeText.swift, RefreshButton.swift, TutorialOverlay.swift, or StatusBarManager.swift — these are either outside the popover section concern or have unique animation needs (e.g., RefreshButton uses 0.5s, MarqueeText uses linear).

After migration, verify: no remaining `easeInOut(duration: 0.2)` or `easeInOut(duration: 0.15)` literals in CollapsibleSectionHeader.swift or UsagePopoverView.swift (except any that are inside comments).
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -5 && echo "---" && grep -n "easeInOut(duration:" AIBattery/Views/CollapsibleSectionHeader.swift AIBattery/Views/UsagePopoverView.swift 2>/dev/null | wc -l</automated>
  </verify>
  <acceptance_criteria>
- `grep -c "MotionConstants.standard" AIBattery/Views/CollapsibleSectionHeader.swift` returns 1
- `grep -c "MotionConstants" AIBattery/Views/UsagePopoverView.swift` returns at least 4
- `grep -c "easeInOut(duration:" AIBattery/Views/CollapsibleSectionHeader.swift` returns 0
- `grep -c "easeInOut(duration:" AIBattery/Views/UsagePopoverView.swift` returns 0
- `swift build` succeeds with zero errors
  </acceptance_criteria>
  <done>All animation duration literals in CollapsibleSectionHeader.swift and UsagePopoverView.swift replaced with MotionConstants.standard or MotionConstants.snappy. No inline duration literals remain. Build passes.</done>
</task>

</tasks>

<verification>
1. `swift build` passes — all files compile
2. `swift test` passes — full test suite green (no regressions)
3. `grep -rn "easeInOut(duration:" AIBattery/Views/CollapsibleSectionHeader.swift AIBattery/Views/UsagePopoverView.swift AIBattery/Views/TokenHealthSection.swift AIBattery/Views/ProjectUsageSection.swift` — zero matches
4. `grep -rn "contentTransition(.numericText())" AIBattery/Views/` — at least 7 matches
5. `grep -rn "\.transition(.opacity)" AIBattery/Views/TokenHealthSection.swift AIBattery/Views/ProjectUsageSection.swift AIBattery/Views/ActivityChartView.swift` — at least 4 matches
6. `grep -rn "MotionConstants\." AIBattery/Views/` — at least 10 matches across files
</verification>

<success_criteria>
- All collapsible sections (TokenHealth, ProjectUsage, ActivityChart) fade content in/out on expand/collapse
- Numeric values use .contentTransition(.numericText()) for smooth digit transitions
- All inline animation duration literals in popover view files replaced with MotionConstants references
- Full test suite passes with zero regressions
- No #available guards needed (all APIs are macOS 13+)
</success_criteria>

<output>
After completion, create `.planning/phases/07-visual-polish/07-02-SUMMARY.md`
</output>
