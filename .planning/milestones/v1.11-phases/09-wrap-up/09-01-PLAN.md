---
phase: 09-wrap-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/ARCHITECTURE.md
  - spec/CONSTANTS.md
  - spec/UI_SPEC.md
autonomous: true
requirements:
  - CQ-02
  - PG-01

must_haves:
  truths:
    - "ARCHITECTURE.md project tree lists Typography.swift, Spacing.swift, and StyledDivider.swift in the correct directories"
    - "ARCHITECTURE.md project tree lists all Phase-8-extracted view files (PopoverHeaderView, MetricToggleView, PopoverStateViews, PopoverFooterView, UsageGateViews, InsightsCharts, InsightsTrendCostSection, InsightsRowsAndHover, ActivityChartData, ActivityChartTrend)"
    - "CONSTANTS.md has a Design Tokens section covering Typography, Spacing, Layout, and MotionConstants enums"
    - "UI_SPEC.md references design token enums and StyledDivider component"
    - "UI_SPEC.md auto mode button description matches code (static green styling, no repeatForever pulse)"
    - "PG-01 animation safety is documented with audit evidence"
  artifacts:
    - path: "spec/ARCHITECTURE.md"
      provides: "Updated project tree with all Phase 6-8 files"
      contains: "Typography.swift"
    - path: "spec/CONSTANTS.md"
      provides: "Design Tokens section with all enum values"
      contains: "Design Tokens"
    - path: "spec/UI_SPEC.md"
      provides: "Design token references and corrected auto mode description"
      contains: "MotionConstants"
  key_links:
    - from: "spec/CONSTANTS.md"
      to: "AIBattery/Utilities/Typography.swift"
      via: "Token table mirrors enum values"
      pattern: "Typography"
    - from: "spec/CONSTANTS.md"
      to: "AIBattery/Utilities/Spacing.swift"
      via: "Token table mirrors enum values"
      pattern: "Spacing|Layout|MotionConstants"
---

<objective>
Update all three spec files (ARCHITECTURE.md, CONSTANTS.md, UI_SPEC.md) to reflect structural changes from Phases 6-8, and document the PG-01 animation safety audit confirming all animations are inert when the panel is hidden.

Purpose: Close the spec-code gap opened by three phases of production changes, satisfying the spec-driven workflow convention.
Output: Three updated spec files reflecting the current codebase state.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-wrap-up/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update ARCHITECTURE.md and CONSTANTS.md with Phase 6-8 structural changes</name>
  <files>spec/ARCHITECTURE.md, spec/CONSTANTS.md</files>
  <read_first>
    - spec/ARCHITECTURE.md
    - spec/CONSTANTS.md
    - AIBattery/Utilities/Typography.swift
    - AIBattery/Utilities/Spacing.swift
    - AIBattery/Views/StyledDivider.swift
    - .planning/phases/09-wrap-up/09-RESEARCH.md
  </read_first>
  <action>
**ARCHITECTURE.md — Update project tree (targeted additions, do not rewrite existing entries):**

1. In the `Utilities/` section (after `ThemeColors.swift` line ~142), add these two entries:
   ```
       Typography.swift             — Named font style tokens (sectionHeader, monoValue, tinyLabel, decorativeIcon, etc.)
       Spacing.swift                — Spacing/Layout/MotionConstants enums + sectionPadding() View extension
   ```

2. In the `Views/` section, add `StyledDivider.swift` after `CollapsibleSectionHeader.swift` (line ~127):
   ```
       StyledDivider.swift          — Standardized divider: Divider() at 0.3 opacity, Spacing.tight vertical padding
   ```

3. Verify the Phase-8-extracted view files are already listed (they should be from Phase 8 summaries): PopoverHeaderView.swift, MetricToggleView.swift, PopoverStateViews.swift, PopoverFooterView.swift, UsageGateViews.swift, InsightsCharts.swift, InsightsTrendCostSection.swift, InsightsRowsAndHover.swift, ActivityChartData.swift, ActivityChartTrend.swift, TokenHealthSessionInfo.swift. If any are missing, add them in alphabetical position within the Views/ section.

4. In the `Tests/AIBatteryCoreTests/` section, verify the Utilities/ subsection includes entries for design system snapshot tests if they exist. Check `Tests/AIBatteryCoreTests/Utilities/` for any Typography/Spacing test files and add entries if present but unlisted.

**CONSTANTS.md — Add Design Tokens section:**

1. Add a new `## Design Tokens` section after the existing `## Animations` section (or at end of file if no Animations section). Use this exact content:

```markdown
## Design Tokens

Canonical Swift constants backing the numeric values in the tables above. Defined as caseless enum namespaces in `Utilities/`.

### Typography (`Utilities/Typography.swift`)

| Token | Value |
|-------|-------|
| `sectionHeader` | `.subheadline.bold()` |
| `chevronIcon` | `.system(size: 9, weight: .bold)` |
| `heroTitle` | `.system(size: 14)` |
| `heroValue` | `.system(size: 12, weight: .bold)` |
| `bodyLabel` | `.system(size: 11, weight: .medium)` |
| `caption` | `.caption` |
| `tinyLabel` | `.caption2` |
| `monoValue` | `.system(.headline, design: .monospaced, weight: .semibold)` |
| `monoValueMedium` | `.system(.subheadline, design: .monospaced, weight: .semibold)` |
| `monoCaption` | `.system(.caption, design: .monospaced)` |
| `monoCaptionSmall` | `.system(.caption2, design: .monospaced)` |
| `monoTiny` | `.system(size: 9, design: .monospaced)` |
| `badgeLabel` | `.system(size: 9, weight: .medium, design: .monospaced)` |
| `buttonLabel` | `.subheadline.weight(.medium)` |
| `decorativeIcon` | `.system(size: 8)` — 8pt minimum (UI-05) |

### Spacing (`Utilities/Spacing.swift`)

| Token | Value | Usage |
|-------|-------|-------|
| `Spacing.tight` | 2pt | Divider micro-gap, dot gap |
| `Spacing.small` | 4pt | Badge internal padding, minor offset |
| `Spacing.gap` | 6pt | VStack section spacing, header/footer V padding |
| `Spacing.section` | 8pt | Standard section vertical outer padding |
| `Spacing.sectionHorizontal` | 16pt | Standard section horizontal outer padding |
| `Spacing.overlay` | 24pt | Overlay and tutorial content padding |

### Layout (`Utilities/Spacing.swift`, co-located)

| Token | Value | Usage |
|-------|-------|-------|
| `Layout.popoverWidth` | 275pt | AuthView, StatusBarManager panel width |
| `Layout.chartHeight` | 50pt | ActivityChartView height |
| `Layout.barHeight` | 8pt | UsageBar, TokenHealthSection progress bar |
| `Layout.barCornerRadius` | 3pt | Progress bar corner radius |
| `Layout.chevronFrame` | 22pt | CollapsibleSectionHeader chevron tap target |
| `Layout.dotSize` | 8pt | Health/model status dot diameter |
| `Layout.dotSizeSmall` | 6pt | Token type/status component dot diameter |

### MotionConstants (`Utilities/Spacing.swift`, co-located)

| Token | Value | Usage |
|-------|-------|-------|
| `MotionConstants.standard` | `.easeInOut(duration: 0.2)` | Section expand/collapse, settings toggle, account switch |
| `MotionConstants.snappy` | `.easeInOut(duration: 0.15)` | Session navigation, metric mode change |
```

2. Cross-reference: If the existing `## UI Layout` or `## Animations` sections list the same raw numeric values, add a brief note at the top of those sections: "See also: Design Tokens section for the Swift enum constants backing these values."

**Verify the token values match the actual source files** — read Typography.swift and Spacing.swift to confirm each value before writing. If any value differs from the research, use the actual source code value.
  </action>
  <verify>
    <automated>grep -c "Typography.swift" spec/ARCHITECTURE.md && grep -c "Spacing.swift" spec/ARCHITECTURE.md && grep -c "StyledDivider.swift" spec/ARCHITECTURE.md && grep -c "Design Tokens" spec/CONSTANTS.md && grep -c "MotionConstants" spec/CONSTANTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Typography.swift" spec/ARCHITECTURE.md` returns a line in the Utilities/ section
    - `grep "Spacing.swift" spec/ARCHITECTURE.md` returns a line in the Utilities/ section
    - `grep "StyledDivider.swift" spec/ARCHITECTURE.md` returns a line in the Views/ section
    - `grep "## Design Tokens" spec/CONSTANTS.md` returns at least one match
    - `grep "MotionConstants" spec/CONSTANTS.md` returns at least one match
    - `grep "Typography" spec/CONSTANTS.md` returns at least one match
    - `grep "decorativeIcon" spec/CONSTANTS.md` returns a match (8pt minimum token)
    - All token values in CONSTANTS.md match actual enum values in source files
  </acceptance_criteria>
  <done>ARCHITECTURE.md project tree includes all Phase 6-8 files. CONSTANTS.md has a complete Design Tokens section with Typography, Spacing, Layout, and MotionConstants tables whose values match the source code.</done>
</task>

<task type="auto">
  <name>Task 2: Update UI_SPEC.md with design token references, auto mode correction, and PG-01 audit note</name>
  <files>spec/UI_SPEC.md</files>
  <read_first>
    - spec/UI_SPEC.md
    - AIBattery/Views/MetricToggleView.swift
    - .planning/phases/09-wrap-up/09-RESEARCH.md
  </read_first>
  <action>
**UI_SPEC.md — Targeted amendments (do not rewrite existing section content):**

1. **Add Design Tokens subsection.** Near the top of the file (after the "Popover Layout" section or at the start of the view hierarchy area), add a short subsection:

```markdown
## Design Tokens

All font sizes, spacing values, layout dimensions, and animation durations are defined as named constants in `Utilities/`:
- **Typography** — 15 named font styles (e.g., `Typography.sectionHeader`, `Typography.monoValue`, `Typography.tinyLabel`)
- **Spacing** — 6 spacing constants (`tight` 2pt, `small` 4pt, `gap` 6pt, `section` 8pt, `sectionHorizontal` 16pt, `overlay` 24pt)
- **Layout** — 7 dimension constants (`popoverWidth` 275pt, `chartHeight` 50pt, `barHeight` 8pt, etc.)
- **MotionConstants** — 2 animation durations (`standard` 0.2s easeInOut, `snappy` 0.15s easeInOut)
- **sectionPadding()** — View extension applying `Spacing.sectionHorizontal` + `Spacing.section` outer padding

Full token values are listed in `CONSTANTS.md > Design Tokens`.
```

2. **Add StyledDivider note.** In or near the section that describes dividers between popover sections, add: "All visual section dividers use `StyledDivider` — a shared component rendering `Divider()` at 0.3 opacity with `Spacing.tight` (2pt) vertical padding."

3. **Fix auto mode button description.** Find the description of the auto mode button (search for `repeatForever` or `autoGlowing` or `pulse` in the context of auto mode). Replace the inaccurate `repeatForever` pulse animation description with: "When auto mode is active, the button uses a green fill, stroke, and shadow (static — no pulse animation). Controlled by the `autoMetricMode` boolean. The glow is a constant shadow effect, not a `repeatForever` animation."

4. **Add animation lifecycle note in the Animations area.** Find the `## Animations` section (or equivalent). Add a paragraph:

```markdown
### Panel Visibility Safety (PG-01)

All popover animations are naturally gated by SwiftUI's view lifecycle. When the panel is hidden (`orderOut`), `NSHostingView` removes all views from the rendering tree — no layout passes occur, no `.animation()` modifiers evaluate, and no `withAnimation` blocks can be triggered (no user interaction possible). `MarqueeText` explicitly calls `cancelAndStop()` in `.onDisappear`. The `StatusBarManager` breath timer operates at the AppKit layer outside the `NSHostingView` and is not a popover animation. Verified by code audit — no runtime gating logic is needed.
```

5. **Verify no `repeatForever` references remain** in UI_SPEC.md after the correction. If any other mentions of a pulsing auto mode animation exist, correct them to say "static green styling."
  </action>
  <verify>
    <automated>grep -c "Design Tokens" spec/UI_SPEC.md && grep -c "MotionConstants" spec/UI_SPEC.md && grep -c "StyledDivider" spec/UI_SPEC.md && grep -c "PG-01" spec/UI_SPEC.md && test $(grep -c "repeatForever" spec/UI_SPEC.md) -eq 0 && echo "no repeatForever references" && swift test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## Design Tokens" spec/UI_SPEC.md` returns at least one match
    - `grep "MotionConstants" spec/UI_SPEC.md` returns at least one match
    - `grep "StyledDivider" spec/UI_SPEC.md` returns at least one match
    - `grep "PG-01" spec/UI_SPEC.md` returns at least one match (animation audit note)
    - `grep "repeatForever" spec/UI_SPEC.md` returns zero matches (inaccurate pulse description removed)
    - `grep "static.*green\|green.*static\|no pulse" spec/UI_SPEC.md` returns at least one match (corrected auto mode description)
    - `swift test` passes with no regressions (spec-only changes should not affect tests)
  </acceptance_criteria>
  <done>UI_SPEC.md references design token enums, documents StyledDivider, corrects the auto mode button description to match code (static green, no pulse), and includes the PG-01 animation safety audit note. All tests pass.</done>
</task>

</tasks>

<verification>
1. All three spec files updated: `ls -la spec/ARCHITECTURE.md spec/CONSTANTS.md spec/UI_SPEC.md`
2. ARCHITECTURE.md has Phase 6-8 files: `grep -E "Typography|Spacing\.swift|StyledDivider" spec/ARCHITECTURE.md`
3. CONSTANTS.md has design tokens: `grep "Design Tokens" spec/CONSTANTS.md`
4. UI_SPEC.md references tokens: `grep "MotionConstants" spec/UI_SPEC.md`
5. No stale repeatForever in UI_SPEC: `grep -c "repeatForever" spec/UI_SPEC.md` should be 0
6. PG-01 documented: `grep "PG-01" spec/UI_SPEC.md`
7. Full test suite passes: `swift test`
</verification>

<success_criteria>
- CQ-02 satisfied: All three spec files reflect Phase 6-8 structural changes (design tokens, extracted views, new utilities)
- PG-01 satisfied: Animation safety audit documented in UI_SPEC.md with evidence that all animations are gated by SwiftUI view lifecycle
- No test regressions (spec-only changes)
- Auto mode button spec matches actual code (static green, no pulse)
</success_criteria>

<output>
After completion, create `.planning/phases/09-wrap-up/09-01-SUMMARY.md`
</output>
