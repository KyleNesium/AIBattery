---
phase: 08-file-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIBattery/Views/UsagePopoverView.swift
  - AIBattery/Views/PopoverHeaderView.swift
  - AIBattery/Views/MetricToggleView.swift
  - AIBattery/Views/PopoverStateViews.swift
  - AIBattery/Views/PopoverFooterView.swift
autonomous: true
requirements: [CQ-01]

must_haves:
  truths:
    - "UsagePopoverView.swift is under 400 lines"
    - "All 4 new sub-view files exist and compile"
    - "Popover renders identically to before extraction"
    - "All existing tests pass without modification"
  artifacts:
    - path: "AIBattery/Views/UsagePopoverView.swift"
      provides: "Thin orchestrator wiring sub-views"
      max_lines: 400
    - path: "AIBattery/Views/PopoverHeaderView.swift"
      provides: "Header section + account picker + update banner"
      max_lines: 400
    - path: "AIBattery/Views/MetricToggleView.swift"
      provides: "Metric picker + auto mode button"
      max_lines: 400
    - path: "AIBattery/Views/PopoverStateViews.swift"
      provides: "Loading/error/empty/idle state placeholders"
      max_lines: 400
    - path: "AIBattery/Views/PopoverFooterView.swift"
      provides: "Footer links + logout + status + timestamp"
      max_lines: 400
  key_links:
    - from: "AIBattery/Views/UsagePopoverView.swift"
      to: "PopoverHeaderView, MetricToggleView, PopoverStateViews, PopoverFooterView"
      via: "init parameter passing"
      pattern: "PopoverHeaderView\\(|MetricToggleView\\(|PopoverFooterView\\(|loadingView|errorView|emptyView"
---

<objective>
Extract UsagePopoverView.swift (666 lines) into 4 focused sub-view files plus a thin orchestrator, each under 400 lines.

Purpose: Satisfy CQ-01 — large view files broken into composable, navigable sub-views.
Output: 5 files total (1 reduced original + 4 new), all compiling, all tests green.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-file-extraction/08-RESEARCH.md
@AIBattery/Views/UsagePopoverView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract PopoverHeaderView, MetricToggleView, and PopoverStateViews</name>
  <files>
    AIBattery/Views/PopoverHeaderView.swift,
    AIBattery/Views/MetricToggleView.swift,
    AIBattery/Views/PopoverStateViews.swift,
    AIBattery/Views/UsagePopoverView.swift
  </files>
  <read_first>
    AIBattery/Views/UsagePopoverView.swift,
    .planning/phases/08-file-extraction/08-RESEARCH.md,
    CLAUDE.md
  </read_first>
  <action>
Extract three new files from UsagePopoverView.swift. Read the full file first to understand exact boundaries.

**PopoverHeaderView.swift** (~120 lines):
- Create `struct PopoverHeaderView: View` as a standalone View struct
- Move `headerSection` (lines ~172-319) content into this struct's body
- Move `accountPicker` computed property and `accountLabel` helper
- Move the `#if ENABLE_VERSION_CHECKER` state vars (`updateCheckMessage`, `updateCheckDismissTask`, `updateBannerDismissed`) and the update banner view code
- Init params: `snapshot: UsageSnapshot?`, `accountStore: AccountStore` (as `@ObservedObject`), `showSettings: Binding<Bool>`, `isAddingAccount: Binding<Bool>`, `viewModel: UsageViewModel` (for `viewModel.refresh()` call in banner)
- Keep `import SwiftUI` at top

**MetricToggleView.swift** (~75 lines):
- Create `struct MetricToggleView: View` as a standalone View struct
- Move `metricToggle` (lines ~452-510), `autoModeButton`, `announceAutoMode`, `cachedOrderedModes` state, `orderedModes`, `recomputeOrderedModes`
- Re-declare `@AppStorage(UserDefaultsKeys.autoMetricMode) private var autoMetricMode: Bool = false` (same key, works across files)
- Init params: `metricModeRaw: Binding<String>`, `autoResolvedBinding: Binding<String>`, `autoMetricMode: Bool` (read from parent), `snapshot: UsageSnapshot?`
- Note: `autoModeButton` stays as a `private var` inside this struct (not a separate type)

**PopoverStateViews.swift** (~75 lines):
- Create standalone structs: `PopoverLoadingView`, `PopoverErrorView`, `PopoverEmptyView`, `PopoverIdleFilteredView`
- Move `loadingView` (lines ~373-384) into `PopoverLoadingView` body
- Move `errorView` (lines ~385-411) into `PopoverErrorView` with `message: String` and `onRetry: () -> Void` init params
- Move `emptyView` (lines ~412-426) into `PopoverEmptyView` body
- Move `idleFilteredEmptyState` (lines ~427-441) into `PopoverIdleFilteredView` with `idleSessionMinutes: Double` init param
- These are pure display views with no bindings needed

**Update UsagePopoverView.swift:**
- Remove the moved code sections
- Replace with calls to the new sub-views: `PopoverHeaderView(...)`, `MetricToggleView(...)`, `PopoverLoadingView()`, `PopoverErrorView(message:onRetry:)`, `PopoverEmptyView()`, `PopoverIdleFilteredView(idleSessionMinutes:)`
- Remove the `#if ENABLE_VERSION_CHECKER` state vars (moved to PopoverHeaderView)
- Remove `cachedOrderedModes`, `orderedModes`, `recomputeOrderedModes` (moved to MetricToggleView)
- Keep: struct declaration, `viewModel`, `accountStore`, `showSettings`, `isAddingAccount`, `metricModeRaw`, `autoMetricMode`, `accountCountAtAddStart`, `showLogoutConfirm`, `logoutRevertTask`, `metricMode`, `autoResolvedBinding`, `body`, `mainContent`
- Keep `footerSection` in UsagePopoverView for now (extracted in Task 2)

After extraction, verify build compiles: `swift build 2>&1 | tail -20`

IMPORTANT: `private` is file-scoped in Swift. Any helper that was `private` in UsagePopoverView and needs to be called from the new file must either (a) move to the new file as `private`, or (b) become `internal` (remove `private` keyword). Prefer moving helpers into the file that uses them.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - PopoverHeaderView.swift exists and contains `struct PopoverHeaderView: View`
    - MetricToggleView.swift exists and contains `struct MetricToggleView: View`
    - PopoverStateViews.swift exists and contains `PopoverLoadingView`, `PopoverErrorView`, `PopoverEmptyView`, `PopoverIdleFilteredView`
    - UsagePopoverView.swift no longer contains `private var headerSection`, `private var metricToggle`, `private var loadingView`, `private func errorView`
    - `swift build` succeeds with zero errors
  </acceptance_criteria>
  <done>Three new files created, UsagePopoverView reduced, project compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Extract PopoverFooterView and verify final line counts</name>
  <files>
    AIBattery/Views/PopoverFooterView.swift,
    AIBattery/Views/UsagePopoverView.swift
  </files>
  <read_first>
    AIBattery/Views/UsagePopoverView.swift,
    AIBattery/Views/PopoverFooterView.swift (if exists from Task 1 error — should not exist yet)
  </read_first>
  <action>
Extract the footer section from UsagePopoverView.swift (post-Task-1 state).

**PopoverFooterView.swift** (~155 lines):
- Create `struct PopoverFooterView: View` as a standalone View struct
- Move `footerSection` (lines ~512-666 in original) content into this struct
- Move supporting helpers: `systemIndicator`, `statusColor`, `relativeTime` (static), `absoluteTime` (static), `absoluteFormatter` (static), `statusTooltip`
- Init params: `systemStatus: StatusIndicator?` (computed from viewModel.snapshot), `isLoading: Bool`, `lastFreshFetch: Date?`, `showLogoutConfirm: Binding<Bool>`, `onLogout: () -> Void`, `snapshot: UsageSnapshot?`, `isAuthenticated: Bool`, `statusText: String?`
- Adjust param list as needed to capture all data the footer reads from viewModel — read the actual code to determine exact dependencies

**Update UsagePopoverView.swift:**
- Remove `footerSection` and all supporting private helpers that moved
- Replace with `PopoverFooterView(...)` call, passing required data from viewModel
- Keep `showLogoutConfirm` and `logoutRevertTask` @State in UsagePopoverView, pass as Binding/callback

**Final verification:**
- `swift build` must succeed
- `swift test` must pass (full regression)
- `wc -l` on all 5 files must show each under 400 lines

Run: `swift build 2>&1 | tail -20` then `swift test 2>&1 | tail -30` then `wc -l AIBattery/Views/UsagePopoverView.swift AIBattery/Views/PopoverHeaderView.swift AIBattery/Views/MetricToggleView.swift AIBattery/Views/PopoverStateViews.swift AIBattery/Views/PopoverFooterView.swift`
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift test 2>&1 | tail -30 && wc -l AIBattery/Views/UsagePopoverView.swift AIBattery/Views/PopoverHeaderView.swift AIBattery/Views/MetricToggleView.swift AIBattery/Views/PopoverStateViews.swift AIBattery/Views/PopoverFooterView.swift</automated>
  </verify>
  <acceptance_criteria>
    - PopoverFooterView.swift exists and contains `struct PopoverFooterView: View`
    - UsagePopoverView.swift no longer contains `private var footerSection`, `relativeTime`, `absoluteFormatter`
    - All 5 files are under 400 lines each (verified via wc -l)
    - `swift test` passes with zero failures
  </acceptance_criteria>
  <done>UsagePopoverView fully extracted into 4 sub-views + thin orchestrator, all under 400 lines, all tests green</done>
</task>

</tasks>

<verification>
- `swift build` compiles without errors
- `swift test` passes all existing tests (no behavioral regressions)
- `wc -l AIBattery/Views/UsagePopoverView.swift` shows under 400 lines
- All 4 new files under 400 lines each
- No `private var headerSection`, `private var footerSection`, `private var metricToggle` remain in UsagePopoverView.swift
</verification>

<success_criteria>
UsagePopoverView.swift reduced from 666 lines to ~165 lines. Four new focused sub-view files created, all under 400 lines. Project compiles and all tests pass unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-extraction/08-01-SUMMARY.md`
</output>
