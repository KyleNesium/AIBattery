---
phase: 11-code-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIBattery/Utilities/Spacing.swift
  - AIBattery/Views/PopoverStateViews.swift
  - spec/CONSTANTS.md
  - spec/UI_SPEC.md
  - spec/ARCHITECTURE.md
  - README.md
autonomous: true
requirements: [CQ-03, CQ-04, CQ-05]

must_haves:
  truths:
    - "sectionPadding() does not exist anywhere in production code"
    - "PopoverLoadingView does not exist anywhere in production code"
    - "CONSTANTS.md MotionConstants values match actual code (0.15s easeOut standard, 0.1s easeOut snappy)"
    - "UI_SPEC.md no longer references sectionPadding() or stale animation values"
    - "README test coverage section is accurate (matches swift test output)"
  artifacts:
    - path: "AIBattery/Utilities/Spacing.swift"
      provides: "Spacing/Layout/MotionConstants without dead sectionPadding extension"
      contains: "enum MotionConstants"
    - path: "AIBattery/Views/PopoverStateViews.swift"
      provides: "Error/Empty/IdleFiltered state views without dead PopoverLoadingView"
      contains: "struct PopoverErrorView"
    - path: "spec/CONSTANTS.md"
      provides: "Accurate animation constants"
      contains: "easeOut(duration: 0.15)"
    - path: "spec/UI_SPEC.md"
      provides: "Accurate design token documentation"
  key_links:
    - from: "spec/CONSTANTS.md"
      to: "AIBattery/Utilities/Spacing.swift"
      via: "MotionConstants values must match"
      pattern: "0\\.15.*easeOut"
---

<objective>
Remove dead code (sectionPadding(), PopoverLoadingView) and sync all specs and README with current codebase state.

Purpose: Close out v1.12 milestone with a clean codebase — no orphaned code, no stale docs.
Output: Clean source files, accurate specs, accurate README.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-popover-performance/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove dead code and sync specs</name>
  <files>AIBattery/Utilities/Spacing.swift, AIBattery/Views/PopoverStateViews.swift, spec/CONSTANTS.md, spec/UI_SPEC.md, spec/ARCHITECTURE.md</files>
  <read_first>
    - AIBattery/Utilities/Spacing.swift (lines 70-83 contain sectionPadding to remove)
    - AIBattery/Views/PopoverStateViews.swift (lines 1-17 contain PopoverLoadingView to remove)
    - spec/CONSTANTS.md (full file — find stale MotionConstants and Animations sections)
    - spec/UI_SPEC.md (full file — find stale sectionPadding reference and design tokens)
    - spec/ARCHITECTURE.md (grep for sectionPadding and PopoverLoadingView references)
  </read_first>
  <action>
**Dead code removal (CQ-03):**

1. In `AIBattery/Utilities/Spacing.swift`: Delete lines 70-83 (the `// MARK: - View convenience extension` block through closing brace). This removes the entire `sectionPadding()` View extension. Leave the rest of the file untouched (Spacing, Layout, MotionConstants enums).

2. In `AIBattery/Views/PopoverStateViews.swift`: Delete lines 1-17 (the `import SwiftUI` through the closing brace of `PopoverLoadingView`, plus the blank line after). The file should now start with `// MARK: - Error` and the `PopoverErrorView` struct. Keep the existing `import SwiftUI` at the top (the file still needs it for the remaining views). Result: file starts with `import SwiftUI` then `// MARK: - Error`.

**Spec sync (CQ-04):**

3. In `spec/CONSTANTS.md`, update the MotionConstants table (around line 332-337):
   - Change `MotionConstants.standard` from `.easeInOut(duration: 0.2)` to `.easeOut(duration: 0.15)` with usage "Standard section expand/collapse, settings toggle, account switch"
   - Change `MotionConstants.snappy` from `.easeInOut(duration: 0.15)` to `.easeOut(duration: 0.1)` with usage "Session navigation, metric mode change"

4. In `spec/CONSTANTS.md`, update the Animations table (around line 272-280):
   - Settings toggle: change `.easeInOut(duration: 0.2)` to `.easeOut(duration: 0.15)` and update reference to `MotionConstants.standard`
   - Metric mode change: change `.easeInOut(duration: 0.15)` to `.easeOut(duration: 0.1)` and update reference to `MotionConstants.snappy`
   - Account switch: change `.easeInOut(duration: 0.2)` to `.easeOut(duration: 0.15)` and update reference to `MotionConstants.standard`

5. In `spec/UI_SPEC.md`:
   - Remove the `sectionPadding()` bullet from the design tokens list (around line 100)
   - Update the MotionConstants summary line (around line 99) from "0.2s easeInOut, snappy 0.15s easeInOut" to "0.15s easeOut, snappy 0.1s easeOut"

6. In `spec/ARCHITECTURE.md`:
   - Update the Spacing.swift description (around line 148) to remove "sectionPadding() View extension" — change to "Spacing/Layout/MotionConstants enums — caseless enum namespaces co-located"
   - Update PopoverStateViews.swift description (around line 106) to remove "PopoverLoadingView," — change to "PopoverErrorView, PopoverEmptyView, PopoverIdleFilteredView"
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -5 && echo "---" && grep -c "sectionPadding" AIBattery/Utilities/Spacing.swift; echo "sectionPadding count in Spacing.swift (expect 0)" && grep -c "PopoverLoadingView" AIBattery/Views/PopoverStateViews.swift; echo "PopoverLoadingView count in PopoverStateViews.swift (expect 0)" && grep "MotionConstants.standard" spec/CONSTANTS.md && grep "MotionConstants.snappy" spec/CONSTANTS.md</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "sectionPadding" AIBattery/Utilities/Spacing.swift returns 0
    - grep -c "PopoverLoadingView" AIBattery/Views/PopoverStateViews.swift returns 0
    - grep "MotionConstants.standard" spec/CONSTANTS.md contains "easeOut(duration: 0.15)"
    - grep "MotionConstants.snappy" spec/CONSTANTS.md contains "easeOut(duration: 0.1)"
    - grep -c "sectionPadding" spec/UI_SPEC.md returns 0
    - grep -c "sectionPadding" spec/ARCHITECTURE.md returns 0
    - grep -c "PopoverLoadingView" spec/ARCHITECTURE.md returns 0
    - swift build succeeds with no errors
  </acceptance_criteria>
  <done>sectionPadding() and PopoverLoadingView removed from source, all three spec files updated with accurate values, project compiles</done>
</task>

<task type="auto">
  <name>Task 2: Verify and update README test coverage</name>
  <files>README.md</files>
  <read_first>
    - README.md (find the Test Coverage section near line 535)
  </read_first>
  <action>
**README sync (CQ-05):**

Phase 10 executor already updated README to "716 tests across 46 test files". This task verifies accuracy after Task 1's dead code removal (which should not affect test counts since the removed code had no tests).

1. Run `swift test 2>&1 | grep -E "Test run|passed|failed"` to get the actual test count.

2. If the count matches 716/46 — no README change needed, just confirm.

3. If the count differs (e.g., a test referenced PopoverLoadingView), update the Test Coverage section header to match actual counts. Also verify the per-area breakdown table is accurate.

Note: If `swift test` cannot run locally (requires Xcode, not just CLT), verify by grepping test files:
- `find Tests/ -name "*Tests.swift" | wc -l` for file count
- `grep -r "@Test" Tests/ | wc -l` for approximate test count
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && grep "tests\*\* across" README.md && find Tests/ -name "*Tests.swift" | wc -l | tr -d ' ' && echo "test files found"</automated>
  </verify>
  <acceptance_criteria>
    - grep "tests\*\* across" README.md returns a line with accurate counts
    - Test file count from find matches the number in README
  </acceptance_criteria>
  <done>README test coverage section is verified accurate (matches actual test count and file count)</done>
</task>

</tasks>

<verification>
1. `swift build` compiles without errors
2. `grep -r "sectionPadding" AIBattery/` returns 0 results
3. `grep -r "PopoverLoadingView" AIBattery/` returns 0 results
4. `grep "easeOut(duration: 0.15)" spec/CONSTANTS.md` finds MotionConstants.standard
5. `grep "easeOut(duration: 0.1)" spec/CONSTANTS.md` finds MotionConstants.snappy
6. README test coverage numbers match actual test count
</verification>

<success_criteria>
- All dead code removed, project compiles
- All spec files reflect current codebase values
- README test coverage is accurate
- Zero references to removed code in spec/ or source files
</success_criteria>

<output>
After completion, create `.planning/phases/11-code-cleanup/11-01-SUMMARY.md`
</output>
