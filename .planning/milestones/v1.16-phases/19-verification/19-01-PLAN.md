---
phase: 19-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/AIBatteryCoreTests/Services/SessionLogReaderIntegrationTests.swift
  - Tests/AIBatteryCoreTests/Services/UsageAggregatorIntegrationTests.swift
autonomous: false
requirements: [CPU-01, CPU-02]

must_haves:
  truths:
    - "Integration tests verify that unchanged files produce zero re-parses on second aggregation cycle"
    - "Integration tests verify that aggregation totals remain correct after entry eviction"
    - "Integration tests verify that the full pipeline (discover -> read -> aggregate) completes without regression"
    - "CPU stays under 2% at idle with popover closed (human-verified)"
    - "CPU stays under 5% during active polling with popover closed (human-verified)"
  artifacts:
    - path: "Tests/AIBatteryCoreTests/Services/SessionLogReaderIntegrationTests.swift"
      provides: "End-to-end tests for incremental scanning pipeline correctness"
      min_lines: 80
    - path: "Tests/AIBatteryCoreTests/Services/UsageAggregatorIntegrationTests.swift"
      provides: "End-to-end tests for aggregation pipeline with eviction correctness"
      min_lines: 60
  key_links:
    - from: "SessionLogReaderIntegrationTests"
      to: "SessionLogReader.readAllUsageEntries()"
      via: "full pipeline test: discover + parse + cache + re-read"
      pattern: "readAllUsageEntries"
    - from: "UsageAggregatorIntegrationTests"
      to: "UsageAggregator.aggregate()"
      via: "full pipeline test: JSONL fixture -> aggregate -> verify snapshot"
      pattern: "aggregate.*rateLimits"
---

<objective>
Add integration tests that exercise the full incremental scanning + eviction pipeline end-to-end, then human-verify CPU and memory targets on a live build.

Purpose: Prevent regressions in the Phase 17/18 performance work. The existing unit tests cover individual behaviors (dirty flag, eviction, discovery skip) but no test exercises the full pipeline: discover files -> incremental read -> aggregate -> evict -> re-aggregate and confirm totals match.

Output: 2 new test files with integration-level tests, plus human confirmation of CPU/memory targets on real data.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/17-incremental-scanning/17-01-SUMMARY.md
@.planning/phases/17-incremental-scanning/17-02-SUMMARY.md
@.planning/phases/18-memory-optimization/18-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From AIBattery/Services/SessionLogReader.swift:
```swift
final class SessionLogReader: @unchecked Sendable {
    init(projectsURL: URL? = nil)
    func invalidate()
    func readAllUsageEntries() -> [AssistantUsageEntry]
    func discoverJSONLFilesForTesting() -> [URL]
    func expireDiscoveryTTLForTesting()
    func cacheEntriesWithLiveEntriesCountForTesting() -> Int
}
```

From AIBattery/Services/UsageAggregator.swift:
```swift
final class UsageAggregator: @unchecked Sendable {
    init(statsCacheReader: StatsCacheReader, sessionLogReader: SessionLogReader)
    func invalidate()
    func aggregate(rateLimits: RateLimitUsage?, accountId: String?) -> (UsageSnapshot, SideEffects)
}
```

Existing test helpers in SessionLogReaderTests.swift:
```swift
private func makeTempProjectsDir(id: String = UUID().uuidString) throws -> URL
private func writeJSONL(lines: [String], projectName: String, sessionId: String, to dir: URL) throws
private func assistantLine(model: String, inputTokens: Int, outputTokens: Int, ...) -> String
```

Existing test helpers in UsageAggregatorTests.swift:
```swift
private func writeJSONL(_ lines: [String], projectName: String, sessionId: String, to dir: URL) throws
private func makeAssistantLine(model: String, input: Int, output: Int, ...) -> String
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integration tests for incremental scanning pipeline</name>
  <files>Tests/AIBatteryCoreTests/Services/SessionLogReaderIntegrationTests.swift</files>
  <read_first>
    AIBattery/Services/SessionLogReader.swift
    Tests/AIBatteryCoreTests/Services/SessionLogReaderTests.swift
    Tests/AIBatteryCoreTests/Services/SessionLogReaderDiscoveryTests.swift
  </read_first>
  <behavior>
    - Test 1 (fullPipeline_discoverReadInvalidateReread_totalsMatch): Create temp dir with 3 project dirs, each with 2 JSONL files containing known token counts. Call readAllUsageEntries(), verify total entries. Call invalidate(), call readAllUsageEntries() again, verify identical totals (incremental path).
    - Test 2 (fullPipeline_addNewFileAfterFirstRead_picksUpNewEntries): First read returns N entries. Add a new JSONL file to an existing project dir. Invalidate + expire discovery TTL. Second read returns N+M entries where M is the new file's count.
    - Test 3 (fullPipeline_modifyExistingFile_updatedEntriesReflected): First read with file containing 2 entries. Overwrite that file (after Thread.sleep(1) for mod-date change) with 3 entries. Invalidate + expire TTL. Second read reflects the 3 new entries (old 2 removed via stale messageId tracking).
    - Test 4 (fullPipeline_deleteFile_entriesRemoved): First read with 2 files. Delete one file. Invalidate + expire TTL. Second read only has entries from the remaining file.
    - Test 5 (fullPipeline_evictionDoesNotAffectTotals): Create files with old timestamps (yesterday). Read once (triggers eviction of old file entries). Verify cacheEntriesWithLiveEntriesCountForTesting() == 0 for old files. Read again. Totals match first read exactly.
  </behavior>
  <action>
    Create SessionLogReaderIntegrationTests.swift with a @Suite("SessionLogReader Integration", .serialized) test suite. Duplicate the helper functions from SessionLogReaderTests (makeTempProjectsDir, writeJSONL, assistantLine) since they are private to that suite. Each test creates a fresh temp directory, constructs SessionLogReader(projectsURL:), and exercises the full discover -> parse -> cache -> invalidate -> re-read pipeline.

    For the eviction test (Test 5), set the JSONL file's mod date to yesterday using FileManager.setAttributes so evictOldFileEntries() triggers. After the first readAllUsageEntries() call, assert cacheEntriesWithLiveEntriesCountForTesting() == 0. Then call invalidate() + readAllUsageEntries() again and assert the total token count matches.

    For Test 3, use Thread.sleep(1.0) before overwriting the file to ensure APFS mod-date changes (per Phase 17 lesson).

    Use Swift Testing framework (@Test, #expect). Each test cleans up its temp directory via addTeardownBlock or defer.
  </action>
  <verify>
    <automated>swift build --build-tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at Tests/AIBatteryCoreTests/Services/SessionLogReaderIntegrationTests.swift
    - Contains @Suite("SessionLogReader Integration"
    - Contains at least 5 @Test functions
    - Contains fullPipeline_discoverReadInvalidateReread_totalsMatch
    - Contains fullPipeline_evictionDoesNotAffectTotals
    - swift build --build-tests succeeds
  </acceptance_criteria>
  <done>5 integration tests compile successfully, covering the full incremental scanning pipeline: read/re-read, add file, modify file, delete file, and eviction correctness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integration tests for aggregation pipeline with eviction</name>
  <files>Tests/AIBatteryCoreTests/Services/UsageAggregatorIntegrationTests.swift</files>
  <read_first>
    AIBattery/Services/UsageAggregator.swift
    AIBattery/Services/SessionLogReader.swift
    Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift
  </read_first>
  <behavior>
    - Test 1 (fullPipeline_aggregateThenReaggregate_snapshotMatches): Create JSONL fixtures with known token counts across 2 projects. Aggregate. Invalidate both reader and aggregator. Re-aggregate. Verify todayMessages, totalTokens, projectTokens match between snapshots.
    - Test 2 (fullPipeline_aggregateAfterEviction_totalsPreserved): Create JSONL files dated yesterday. Aggregate (triggers reader eviction). Invalidate aggregator only (not reader — simulates next polling cycle where files haven't changed). Re-aggregate. Totals match.
    - Test 3 (fullPipeline_newSessionAdded_aggregateReflectsIt): First aggregate with 1 session file. Add second session file. Invalidate both. Second aggregate shows increased message count and token totals.
  </behavior>
  <action>
    Create UsageAggregatorIntegrationTests.swift with @Suite("UsageAggregator Integration", .serialized). Duplicate helpers (writeJSONL, makeAssistantLine) from UsageAggregatorTests. Each test creates a fresh temp dir, constructs SessionLogReader(projectsURL:) and UsageAggregator(statsCacheReader:sessionLogReader:). Use StatsCacheReader(statsCachePath:) pointed at a non-existent file so stats cache returns nil (JSONL-only aggregation).

    For Test 2, set file mod dates to yesterday so SessionLogReader evicts entry arrays after first read. Then invalidate only the aggregator (not the reader) — the reader's cache is still valid (fingerprints unchanged), so it returns cachedAllEntries directly. Assert the snapshot totals are identical.

    All timestamps in JSONL fixtures must use today's date so they appear in todayEntries. Use ISO8601DateFormatter with fractionalSeconds for timestamp strings.
  </action>
  <verify>
    <automated>swift build --build-tests 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File exists at Tests/AIBatteryCoreTests/Services/UsageAggregatorIntegrationTests.swift
    - Contains @Suite("UsageAggregator Integration"
    - Contains at least 3 @Test functions
    - Contains fullPipeline_aggregateAfterEviction_totalsPreserved
    - swift build --build-tests succeeds
  </acceptance_criteria>
  <done>3 integration tests compile successfully, covering the full aggregation pipeline: re-aggregate consistency, eviction correctness, and new session detection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify CPU and memory targets on live build</name>
  <files>none</files>
  <action>
    Human builds and launches the app, then measures CPU and memory using ps commands. No code changes — pure measurement checkpoint.
  </action>
  <what-built>
    Integration tests for the full incremental scanning and aggregation pipeline (Tasks 1-2). These tests verify correctness programmatically. This checkpoint verifies the runtime performance targets on real data.
  </what-built>
  <how-to-verify>
    1. Build and launch the app:
       ```
       pkill -f AIBattery.app 2>/dev/null
       SPARKLE_EDDSA_PUBLIC_KEY="6OMshMFo6tpWjrJHcDa1xKK4N0xqgT+gery+xnGJrOU=" ./scripts/build-app.sh && open .build/AIBattery.app
       ```

    2. **CPU at idle (CPU-01):** Close the popover. Wait 60 seconds. Run:
       ```
       ps -p $(pgrep -x AIBattery) -o %cpu= 2>/dev/null
       ```
       Expected: under 2%

    3. **CPU during active polling (CPU-02):** With a Claude Code session running (or recently active), close the popover. Wait for a polling cycle (10s). Run:
       ```
       ps -p $(pgrep -x AIBattery) -o %cpu= 2>/dev/null
       ```
       Expected: under 5%

    4. **Memory (regression check):** Run:
       ```
       ps -p $(pgrep -x AIBattery) -o rss= 2>/dev/null
       ```
       Expected: under 100 MB (divide by 1024 to get MB)

    5. **Correctness spot check:** Open the popover. Verify token counts and costs display correctly (non-zero, reasonable values matching your known usage).
  </how-to-verify>
  <verify>
    <automated>MISSING — human measurement checkpoint, no automated verification possible</automated>
  </verify>
  <done>CPU-01 confirmed under 2% at idle, CPU-02 confirmed under 5% during polling, RSS under 100 MB, token counts display correctly.</done>
  <resume-signal>Type "approved" with the CPU/memory numbers, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
- `swift build --build-tests` succeeds with both new test files
- 8 new integration tests cover: incremental re-read, file add/modify/delete, eviction correctness, aggregation consistency, and new session detection
- Human confirms CPU < 2% at idle (CPU-01) and CPU < 5% during polling (CPU-02)
- Human confirms RSS < 100 MB
- Human confirms token counts display correctly (no correctness regression)
</verification>

<success_criteria>
1. SessionLogReaderIntegrationTests.swift compiles with 5 tests covering full pipeline scenarios
2. UsageAggregatorIntegrationTests.swift compiles with 3 tests covering aggregation + eviction
3. CPU confirmed under 2% at idle by human measurement
4. CPU confirmed under 5% during active polling by human measurement
5. RSS confirmed under 100 MB by human measurement
6. Token counts and costs display correctly (no regression)
</success_criteria>

<output>
After completion, create `.planning/phases/19-verification/19-01-SUMMARY.md`
</output>
