---
phase: 08-file-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - AIBattery/Views/ActivityChartView.swift
  - AIBattery/Views/InsightsCharts.swift
  - AIBattery/Views/InsightsTrendCostSection.swift
  - AIBattery/Views/InsightsRowsAndHover.swift
autonomous: true
requirements: [CQ-01]

must_haves:
  truths:
    - "ActivityChartView.swift is under 400 lines"
    - "All 3 new extension files exist and compile"
    - "Activity charts render identically to before extraction"
    - "All existing tests pass without modification"
    - "ActivityChartTrend.swift still resolves InsightsView.formatHourLabel etc. without changes"
  artifacts:
    - path: "AIBattery/Views/ActivityChartView.swift"
      provides: "InsightsView core struct with state, cache, fingerprinting, body"
      max_lines: 400
    - path: "AIBattery/Views/InsightsCharts.swift"
      provides: "extension InsightsView with dailyChart, hourlyChart, monthlyChart, shared styling"
      max_lines: 400
    - path: "AIBattery/Views/InsightsTrendCostSection.swift"
      provides: "extension InsightsView with trendSummary, trendRows, costSection"
      max_lines: 400
    - path: "AIBattery/Views/InsightsRowsAndHover.swift"
      provides: "extension InsightsView with insightRows, hover helpers, static formatters"
      max_lines: 400
  key_links:
    - from: "AIBattery/Views/ActivityChartTrend.swift"
      to: "InsightsView static formatters"
      via: "InsightsView.formatHourLabel, InsightsView.monthAbbrev, InsightsView.compactCount"
      pattern: "InsightsView\\.formatHourLabel|InsightsView\\.monthAbbrev|InsightsView\\.compactCount"
---

<objective>
Extract ActivityChartView.swift (711 lines) into 3 extension files plus a core struct, each under 400 lines.

Purpose: Satisfy CQ-01 — large view files broken into composable, navigable sub-views.
Output: 4 files total (1 reduced original + 3 new extensions), all compiling, all tests green.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-file-extraction/08-RESEARCH.md
@AIBattery/Views/ActivityChartView.swift
@AIBattery/Views/ActivityChartTrend.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract InsightsCharts and InsightsTrendCostSection extensions</name>
  <files>
    AIBattery/Views/InsightsCharts.swift,
    AIBattery/Views/InsightsTrendCostSection.swift,
    AIBattery/Views/ActivityChartView.swift
  </files>
  <read_first>
    AIBattery/Views/ActivityChartView.swift,
    AIBattery/Views/ActivityChartTrend.swift,
    .planning/phases/08-file-extraction/08-RESEARCH.md,
    CLAUDE.md
  </read_first>
  <action>
Extract chart and trend/cost sections from ActivityChartView.swift using the extension pattern. Read the full file first.

**InsightsCharts.swift** (~235 lines):
- `import SwiftUI` and `import Charts` at top
- Create `extension InsightsView { ... }` containing:
  - `static let areaGradient` and `static let chartLineStyle` (from MARK: Shared chart styling, lines ~186-198)
  - `var sharedYAxis: some AxisContent` (lines ~199-212)
  - `var dailyChart: some View` (lines ~215-288, MARK: Daily Chart)
  - `var hourlyChart: some View` (lines ~291-352, MARK: Hourly Chart)
  - `var monthlyChart: some View` (lines ~355-422, MARK: Monthly Chart)
- These use `@State` properties (selectedDailyId, selectedHourlyOffset, selectedMonthlyId) directly via `self` — no Binding needed because extensions share the type's stored properties
- Change access from `private` to `internal` (just remove `private` keyword) since `private` in an extension in a different file means private to THAT file, not the original type's file. The body in ActivityChartView.swift calls these properties, so they must be `internal`.

**InsightsTrendCostSection.swift** (~120 lines):
- `import SwiftUI` at top
- Create `extension InsightsView { ... }` containing:
  - `func trendSummary(_ snapshot: UsageSnapshot) -> some View` (lines ~426-435, MARK: Trend)
  - `func trendRowTop(change:stat:) -> some View` (lines ~438-456, MARK: Shared trend row builders)
  - `func trendRowBottom(throttleCount:peak:) -> some View` (lines ~457-476)
  - `var windowedModelTokens: [ModelTokenSummary]` (lines ~485-493, MARK: Cost breakdown)
  - `func isActive(_ model: ModelTokenSummary) -> Bool` (lines ~494-499)
  - `func costSection(_ snapshot: UsageSnapshot) -> some View` (lines ~500-539)
  - Cost section column width constants: `costColumnWidth`, `tokenColumnWidth` (if present as local constants, extract them)
- Remove `private` keyword from all moved members (same reason: cross-file extension visibility)

**Update ActivityChartView.swift:**
- Remove the moved sections (everything from MARK: Shared chart styling through MARK: Cost breakdown end)
- Keep: `import SwiftUI`, `import Charts`, `ActivityChartMode` enum, `InsightsView` struct declaration with all `@State`/`@AppStorage` properties, `refreshCachedData`, `ensureCachedData`, `dataFingerprint`, `isEmpty`, and `body`
- The body references `dailyChart`, `hourlyChart`, `monthlyChart`, `trendSummary`, `costSection` — these resolve via the extensions

CRITICAL: `private` members in a Swift extension in file B are NOT visible from file A, even if they extend the same type. Remove `private` from all extracted members so the body in ActivityChartView.swift can call them. Use `internal` (the default — just remove the keyword).

After extraction, verify: `swift build 2>&1 | tail -20`
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - InsightsCharts.swift exists and contains `extension InsightsView` with `dailyChart`, `hourlyChart`, `monthlyChart`
    - InsightsTrendCostSection.swift exists and contains `extension InsightsView` with `trendSummary`, `costSection`
    - ActivityChartView.swift no longer contains `private var dailyChart`, `private var hourlyChart`, `private var monthlyChart`, `private func trendSummary`, `private func costSection`
    - `swift build` succeeds with zero errors
    - ActivityChartTrend.swift unchanged and compiles (still calls InsightsView.formatHourLabel etc.)
  </acceptance_criteria>
  <done>Two extension files created, ActivityChartView reduced, project compiles cleanly</done>
</task>

<task type="auto">
  <name>Task 2: Extract InsightsRowsAndHover extension, verify final line counts and full test suite</name>
  <files>
    AIBattery/Views/InsightsRowsAndHover.swift,
    AIBattery/Views/ActivityChartView.swift
  </files>
  <read_first>
    AIBattery/Views/ActivityChartView.swift,
    AIBattery/Views/ActivityChartTrend.swift
  </read_first>
  <action>
Extract the remaining insight rows and hover helpers from ActivityChartView.swift (post-Task-1 state).

**InsightsRowsAndHover.swift** (~175 lines):
- `import SwiftUI` and `import Charts` at top
- Create `extension InsightsView { ... }` containing:
  - `func insightRows(_ snapshot: UsageSnapshot) -> some View` (MARK: Insight rows)
  - `func insightRow(icon:label:value:valueColor:) -> some View`
  - `var insightLabelWidth: CGFloat` (if exists as computed property)
  - `func tooltipLabel(_ text: String) -> some View` (MARK: Hover helpers)
  - `func chartHoverOverlay(...)`
  - `func currentHoverX(proxy:plotFrame:) -> CGFloat?`
  - `var hoverTooltipText: String?`
  - `var selectionRuleStyle` (if exists)
  - Static formatters (MARK: Formatters): `static func dayShortLabel`, `static func monthAbbrev`, `static func compactCount`, `static func formatHourLabel`, `static let hourLabels` (if exists)
- Remove `private` from all moved members

CRITICAL for static formatters: `ActivityChartTrend.swift` calls `InsightsView.formatHourLabel`, `InsightsView.monthAbbrev`, `InsightsView.compactCount`. Since these are `static` members on `InsightsView` defined in an extension, they remain accessible as `InsightsView.formatHourLabel` — no call-site changes needed. But they MUST NOT be `private` (use internal/default access).

**Update ActivityChartView.swift:**
- Remove all moved sections
- File should now contain only: `ActivityChartMode` enum, `InsightsView` struct with state properties, cache logic, fingerprinting, and body (~184 lines)

**Final verification:**
- `swift build` must succeed
- `swift test` must pass (full regression)
- `wc -l` on all 4 files must show each under 400 lines
- Verify `ActivityChartTrend.swift` still compiles (grep for InsightsView. calls)

Run: `swift build 2>&1 | tail -20` then `swift test 2>&1 | tail -30` then `wc -l AIBattery/Views/ActivityChartView.swift AIBattery/Views/InsightsCharts.swift AIBattery/Views/InsightsTrendCostSection.swift AIBattery/Views/InsightsRowsAndHover.swift`
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift test 2>&1 | tail -30 && wc -l AIBattery/Views/ActivityChartView.swift AIBattery/Views/InsightsCharts.swift AIBattery/Views/InsightsTrendCostSection.swift AIBattery/Views/InsightsRowsAndHover.swift</automated>
  </verify>
  <acceptance_criteria>
    - InsightsRowsAndHover.swift exists and contains `extension InsightsView` with `insightRows`, `chartHoverOverlay`, `formatHourLabel`, `compactCount`
    - ActivityChartView.swift no longer contains `private func insightRows`, `private func tooltipLabel`, `static func dayShortLabel`, `static func formatHourLabel`
    - All 4 files are under 400 lines each (verified via wc -l)
    - `swift test` passes with zero failures
    - `grep "InsightsView\\.formatHourLabel\\|InsightsView\\.monthAbbrev\\|InsightsView\\.compactCount" AIBattery/Views/ActivityChartTrend.swift` still shows the same 3 call sites (unchanged)
  </acceptance_criteria>
  <done>ActivityChartView fully extracted into 3 extension files + core struct, all under 400 lines, all tests green</done>
</task>

</tasks>

<verification>
- `swift build` compiles without errors
- `swift test` passes all existing tests (no behavioral regressions)
- `wc -l AIBattery/Views/ActivityChartView.swift` shows under 400 lines
- All 3 new files under 400 lines each
- `ActivityChartTrend.swift` compiles without changes (InsightsView.formatHourLabel etc. still resolve)
- No `private var dailyChart`, `private var hourlyChart`, `private func insightRows` remain in ActivityChartView.swift
</verification>

<success_criteria>
ActivityChartView.swift reduced from 711 lines to ~184 lines. Three new extension files created, all under 400 lines. Project compiles and all tests pass unchanged. ActivityChartTrend.swift requires zero modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/08-file-extraction/08-02-SUMMARY.md`
</output>
