---
phase: 06-design-system
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - AIBattery/Views/UsagePopoverView.swift
  - AIBattery/Views/ActivityChartView.swift
  - AIBattery/Views/AuthView.swift
  - AIBattery/Views/ProjectUsageSection.swift
  - AIBattery/Views/TokenHealthSection.swift
  - AIBattery/Views/UsageBarsSection.swift
  - AIBattery/Views/FooterLink.swift
  - AIBattery/Views/CollapsibleSectionHeader.swift
  - AIBattery/Views/TutorialOverlay.swift
  - AIBattery/Views/RefreshButton.swift
  - AIBattery/Views/Settings/SettingsRow.swift
  - AIBattery/Views/Settings/AlertSettingsSection.swift
  - AIBattery/Views/Settings/DisplaySettingsSection.swift
  - AIBattery/Views/Settings/RefreshSettingsSection.swift
  - AIBattery/Views/Settings/LaunchAtLoginSection.swift
  - AIBattery/Views/StatusBarManager.swift
  - AIBattery/Views/TokenHealthSessionInfo.swift
  - AIBattery/Views/UsageGateViews.swift
autonomous: true
requirements: [DS-01, DS-02, DS-03, UI-05]

must_haves:
  truths:
    - "Every .font() call in view files uses a Typography constant — no bare .font(.system(size: N)) or .font(.caption) remain"
    - "Every numeric padding call in view files uses a Spacing constant — no bare .padding(.horizontal, 16) remain"
    - "All popover sections use identical outer padding (Spacing.sectionHorizontal horizontal, Spacing.section vertical)"
    - "No text renders below 8pt — the two size:6 instances are replaced with Typography.decorativeIcon"
    - "All existing tests still pass — migration is purely mechanical, no behavioral changes"
  artifacts:
    - path: "AIBattery/Views/UsagePopoverView.swift"
      provides: "Migrated main popover view"
      contains: "Typography."
    - path: "AIBattery/Views/ActivityChartView.swift"
      provides: "Migrated chart view"
      contains: "Spacing."
    - path: "AIBattery/Views/FooterLink.swift"
      provides: "Fixed 6pt violation (UI-05)"
      contains: "Typography.decorativeIcon"
  key_links:
    - from: "AIBattery/Views/UsagePopoverView.swift"
      to: "AIBattery/Utilities/Typography.swift"
      via: "import (same module)"
      pattern: "Typography\\."
    - from: "AIBattery/Views/UsagePopoverView.swift"
      to: "AIBattery/Utilities/Spacing.swift"
      via: "import (same module)"
      pattern: "Spacing\\.|Layout\\.|sectionPadding"
---

<objective>
Migrate all view files from inline font/spacing literals to Typography, Spacing, and Layout constants.

Purpose: Complete the design system adoption — after this plan, every font and spacing value in the UI goes through the centralized token system. Fixes the two 8pt-minimum violations (UI-05) and unifies all section outer padding (DS-03).
Output: All ~15 view files migrated, zero residual inline literals, all tests green.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-design-system/06-CONTEXT.md
@.planning/phases/06-design-system/06-RESEARCH.md
@.planning/phases/06-design-system/06-01-SUMMARY.md

<interfaces>
<!-- Constants created in Plan 01 — executor uses these tokens -->
From AIBattery/Utilities/Typography.swift:
```swift
enum Typography {
    static let sectionHeader: Font       // .subheadline.bold()
    static let chevronIcon: Font         // .system(size: 9, weight: .bold)
    static let heroTitle: Font           // .system(size: 14)
    static let heroValue: Font           // .system(size: 12, weight: .bold)
    static let bodyLabel: Font           // .system(size: 11, weight: .medium)
    static let caption: Font             // .caption
    static let tinyLabel: Font           // .caption2
    static let monoValue: Font           // .system(.headline, design: .monospaced, weight: .semibold)
    static let monoValueMedium: Font     // .system(.subheadline, design: .monospaced, weight: .semibold)
    static let monoCaption: Font         // .system(.caption, design: .monospaced)
    static let monoCaptionSmall: Font    // .system(.caption2, design: .monospaced)
    static let monoTiny: Font            // .system(size: 9, design: .monospaced)
    static let badgeLabel: Font          // .system(size: 9, weight: .medium, design: .monospaced)
    static let buttonLabel: Font         // .subheadline.weight(.medium)
    static let decorativeIcon: Font      // .system(size: 8) — 8pt floor
}
```

From AIBattery/Utilities/Spacing.swift:
```swift
enum Spacing {
    static let tight: CGFloat = 2
    static let small: CGFloat = 4
    static let gap: CGFloat = 6
    static let section: CGFloat = 8
    static let sectionHorizontal: CGFloat = 16
    static let overlay: CGFloat = 24
}
enum Layout {
    static let popoverWidth: CGFloat = 275
    static let chartHeight: CGFloat = 50
    static let barHeight: CGFloat = 8
    static let barCornerRadius: CGFloat = 3
    static let chevronFrame: CGFloat = 22
    static let dotSize: CGFloat = 8
    static let dotSizeSmall: CGFloat = 6
}
extension View {
    func sectionPadding() -> some View
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all view files — typography, spacing, and layout constants</name>
  <files>
    AIBattery/Views/UsagePopoverView.swift,
    AIBattery/Views/ActivityChartView.swift,
    AIBattery/Views/AuthView.swift,
    AIBattery/Views/ProjectUsageSection.swift,
    AIBattery/Views/TokenHealthSection.swift,
    AIBattery/Views/UsageBarsSection.swift,
    AIBattery/Views/FooterLink.swift,
    AIBattery/Views/CollapsibleSectionHeader.swift,
    AIBattery/Views/TutorialOverlay.swift,
    AIBattery/Views/RefreshButton.swift,
    AIBattery/Views/Settings/SettingsRow.swift,
    AIBattery/Views/Settings/AlertSettingsSection.swift,
    AIBattery/Views/Settings/DisplaySettingsSection.swift,
    AIBattery/Views/Settings/RefreshSettingsSection.swift,
    AIBattery/Views/Settings/LaunchAtLoginSection.swift,
    AIBattery/Views/StatusBarManager.swift,
    AIBattery/Views/TokenHealthSessionInfo.swift,
    AIBattery/Views/UsageGateViews.swift
  </files>
  <read_first>
    - AIBattery/Utilities/Typography.swift (the actual tokens to use — verify names match)
    - AIBattery/Utilities/Spacing.swift (spacing + layout tokens + sectionPadding extension)
    - .planning/phases/06-design-system/06-RESEARCH.md (migration scope table + token map + pitfalls)
    - AIBattery/Views/UsagePopoverView.swift (largest file, ~50 font calls, 2 of the 6pt violations)
    - AIBattery/Views/ActivityChartView.swift (~22 font calls)
    - AIBattery/Views/FooterLink.swift (contains 1 of the 6pt violations)
    - AIBattery/Views/MarqueeText.swift (DO NOT modify — .font(font) is a parameter pass-through)
    - AIBattery/Views/CopyableText.swift (DO NOT modify .font(font) pass-through)
  </read_first>
  <action>
Migrate ALL view files in one atomic pass. Read each file, replace inline literals with tokens, write back. Process files in this order (largest to smallest impact):

**Critical UI-05 fixes (6pt to 8pt):**
- `FooterLink.swift:46` — `.font(.system(size: 6))` becomes `.font(Typography.decorativeIcon)`
- `UsagePopoverView.swift:254` — `.font(.system(size: 6))` becomes `.font(Typography.decorativeIcon)`

**Typography migration rules (apply to ALL view files):**
- `.font(.subheadline.bold())` → `.font(Typography.sectionHeader)`
- `.font(.system(size: 9, weight: .bold))` or `.font(.system(size: 8, weight: .bold))` → `.font(Typography.chevronIcon)`
- `.font(.system(size: 14))` → `.font(Typography.heroTitle)`
- `.font(.system(size: 12, weight: .bold))` → `.font(Typography.heroValue)`
- `.font(.system(size: 11, weight: .medium))` → `.font(Typography.bodyLabel)`
- `.font(.caption)` → `.font(Typography.caption)`
- `.font(.caption2)` → `.font(Typography.tinyLabel)`
- `.font(.system(.headline, design: .monospaced, weight: .semibold))` → `.font(Typography.monoValue)`
- `.font(.system(.subheadline, design: .monospaced, weight: .semibold))` → `.font(Typography.monoValueMedium)`
- `.font(.system(.caption, design: .monospaced))` → `.font(Typography.monoCaption)`
- `.font(.system(.caption2, design: .monospaced))` → `.font(Typography.monoCaptionSmall)`
- `.font(.system(size: 9, design: .monospaced))` → `.font(Typography.monoTiny)`
- `.font(.system(size: 9, weight: .medium, design: .monospaced))` → `.font(Typography.badgeLabel)`
- `.font(.subheadline.weight(.medium))` → `.font(Typography.buttonLabel)`
- `.font(.system(size: 6))` → `.font(Typography.decorativeIcon)` (UI-05 fix)
- `.font(.headline)` → `.font(Typography.sectionHeader)` (or `.font(Typography.monoValue)` if monospaced context — check usage)
- `.font(.caption.weight(.semibold))` → `.font(Typography.caption)` with `.fontWeight(.semibold)` inline modifier (1 occurrence)
- `.font(.system(size: 9, weight: .heavy, design: .rounded))` → keep inline (1 one-off in auto mode label)
- `.font(.system(size: 28))` → keep inline (1 one-off in TutorialOverlay emoji icon)
- `.font(.system(size: 13))` → keep inline (1 one-off in AuthView step number)
- `.font(.title3)` → keep inline (1 one-off in settings)

**Spacing migration rules:**
- `.padding(.horizontal, 16)` → `.padding(.horizontal, Spacing.sectionHorizontal)`
- `.padding(.vertical, 8)` → `.padding(.vertical, Spacing.section)`
- `.padding(.vertical, 6)` (outer section padding) → `.padding(.vertical, Spacing.section)` (bumped to 8pt for DS-03 uniformity)
- `.padding(.vertical, 6)` (inner VStack spacing) → `.padding(.vertical, Spacing.gap)` (keep 6pt for inner spacing)
- `.padding(16)` (AuthView uniform) → `.padding(Spacing.sectionHorizontal)`
- `.padding(24)` → `.padding(Spacing.overlay)`
- `.padding(.horizontal, 4)` or `.padding(4)` → `.padding(.horizontal, Spacing.small)` / `.padding(Spacing.small)`
- `.padding(2)` → `.padding(Spacing.tight)`
- `spacing: 6` (VStack/HStack) → `spacing: Spacing.gap`
- `spacing: 4` → `spacing: Spacing.small`
- `spacing: 2` → `spacing: Spacing.tight`
- `spacing: 8` → `spacing: Spacing.section`

**Layout migration rules:**
- `.frame(width: 275)` → `.frame(width: Layout.popoverWidth)`
- `.frame(height: 50)` → `.frame(height: Layout.chartHeight)` (activity/token health charts)
- `.frame(height: 8)` (progress bars) → `.frame(height: Layout.barHeight)`
- `.cornerRadius(3)` (bars) → `.cornerRadius(Layout.barCornerRadius)`
- `.frame(width: 22, height: 22)` (chevron) → `.frame(width: Layout.chevronFrame, height: Layout.chevronFrame)`
- `.frame(width: 8, height: 8)` (dots) → `.frame(width: Layout.dotSize, height: Layout.dotSize)`
- `.frame(width: 6, height: 6)` (small dots) → `.frame(width: Layout.dotSizeSmall, height: Layout.dotSizeSmall)`

**Where to use `sectionPadding()` convenience:**
Consider using `.sectionPadding()` where both `.padding(.horizontal, Spacing.sectionHorizontal)` and `.padding(.vertical, Spacing.section)` appear together. This is optional — the executor may choose either explicit or convenience form.

**DO NOT modify:**
- `MarqueeText.swift` — `.font(font)` is a parameter pass-through, not a literal
- `CopyableText.swift` — `.font(font)` is a parameter pass-through
- `MenuBarIcon.swift` / `MenuBarIconGeometry.swift` — these are NSImage drawing, not SwiftUI Text

**DS-03 section padding uniformity:**
After migration, these files MUST all use `Spacing.sectionHorizontal` horizontal + `Spacing.section` vertical for outer section padding:
- UsagePopoverView.swift (cost section, status section, model tokens section — currently at .vertical, 6 — bump to Spacing.section)
- ActivityChartView.swift (already correct)
- ProjectUsageSection.swift (already correct)
- TokenHealthSection.swift (already correct)
- UsageBarsSection.swift (already correct)
- SettingsRow.swift (currently at .vertical, 6 — bump to Spacing.section)
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn '\.font(\.system(size: 6)' AIBattery/Views/` returns zero results (UI-05)
    - `grep -rn '\.font(\.caption2)' AIBattery/Views/` returns zero results (migrated to Typography.tinyLabel)
    - `grep -rn '\.font(\.caption)' AIBattery/Views/` returns zero results (migrated to Typography.caption) — note: grep pattern must not match Typography.caption
    - `grep -rn 'padding(.horizontal, 16)' AIBattery/Views/` returns zero results (migrated to Spacing.sectionHorizontal)
    - `grep -rn 'Typography\.' AIBattery/Views/UsagePopoverView.swift` returns 30+ results
    - `grep -rn 'Typography\.decorativeIcon' AIBattery/Views/FooterLink.swift` returns at least 1 result
    - `grep -rn 'Typography\.decorativeIcon' AIBattery/Views/UsagePopoverView.swift` returns at least 1 result
    - `grep -rn 'Spacing\.\|Layout\.' AIBattery/Views/` returns results across multiple files
    - `swift build` succeeds with no errors
  </acceptance_criteria>
  <done>All 148+ font calls and 52+ padding calls migrated to design tokens. Zero bare font/spacing literals in view files (except documented one-offs). Both 6pt violations fixed to 8pt.</done>
</task>

<task type="auto">
  <name>Task 2: Run full test suite and verify zero residual inline literals</name>
  <files>None (verification only)</files>
  <read_first>
    - AIBattery/Views/UsagePopoverView.swift (spot-check Typography/Spacing usage)
    - AIBattery/Views/FooterLink.swift (confirm decorativeIcon replacement)
  </read_first>
  <action>
Run the full test suite to confirm no behavioral regressions from the migration.

Then run a comprehensive residual-literal audit:

1. **Font residuals — must be zero in view files (excluding documented one-offs):**
   ```bash
   grep -rn '\.font(\.system(size:' AIBattery/Views/ | grep -v 'size: 28' | grep -v 'size: 13' | grep -v 'size: 9, weight: .heavy'
   grep -rn '\.font(\.caption)' AIBattery/Views/
   grep -rn '\.font(\.caption2)' AIBattery/Views/
   grep -rn '\.font(\.headline)' AIBattery/Views/
   grep -rn '\.font(\.subheadline' AIBattery/Views/
   ```
   All should return zero results. If any remain, fix them.

2. **Spacing residuals — must be zero for known patterns:**
   ```bash
   grep -rn 'padding(.horizontal, 16)' AIBattery/Views/
   grep -rn 'padding(.vertical, 8)' AIBattery/Views/
   grep -rn 'padding(24)' AIBattery/Views/
   ```
   All should return zero results.

3. **UI-05 verification — absolute zero for size:6:**
   ```bash
   grep -rn 'size: 6' AIBattery/Views/
   ```
   Must return zero results.

4. **DS-03 verification — consistent outer padding:**
   Spot-check that UsagePopoverView cost/status/model sections and SettingsRow all use `Spacing.section` (not `Spacing.gap`) for outer vertical padding.

If any residuals are found, go back and fix them before marking complete.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `swift test` exits 0 — all tests pass including the new Typography/Spacing/Layout tests from Plan 01
    - `grep -rn 'size: 6' AIBattery/Views/` returns zero results
    - `grep -rn '\.font(\.caption2)' AIBattery/Views/` returns zero results
    - `grep -rn '\.font(\.caption)[^A-Z]' AIBattery/Views/` returns zero results (bare .caption, not Typography.caption)
    - `grep -rn 'padding(.horizontal, 16)' AIBattery/Views/` returns zero results
    - `grep -rn 'padding(.vertical, 8)' AIBattery/Views/` returns zero results
  </acceptance_criteria>
  <done>Full test suite green. Zero residual inline font/spacing literals confirmed by grep audit. UI-05 (no text below 8pt) verified. DS-03 (consistent outer padding) verified.</done>
</task>

</tasks>

<verification>
1. `swift test` — full suite passes (no regressions)
2. `grep -rn 'size: 6' AIBattery/Views/` — zero results (UI-05)
3. `grep -rn '\.font(\.caption2)' AIBattery/Views/` — zero results (DS-01 complete)
4. `grep -rn '\.font(\.caption)[^A-Z]' AIBattery/Views/` — zero results (DS-01 complete)
5. `grep -rn 'padding(.horizontal, 16)' AIBattery/Views/` — zero results (DS-02 complete)
6. `grep -rn 'Typography\.' AIBattery/Views/` — results in 15+ files (DS-01 adopted)
7. `grep -rn 'Spacing\.\|Layout\.' AIBattery/Views/` — results in 10+ files (DS-02 adopted)
</verification>

<success_criteria>
- All 148+ inline .font() calls replaced with Typography constants (except 3 documented one-offs)
- All 52+ inline .padding() calls replaced with Spacing/Layout constants
- Both 6pt font violations fixed to 8pt (UI-05)
- All popover sections use uniform outer padding via Spacing.sectionHorizontal + Spacing.section (DS-03)
- Full test suite passes — zero behavioral regressions
- Zero residual inline literals confirmed by grep audit
</success_criteria>

<output>
After completion, create `.planning/phases/06-design-system/06-02-SUMMARY.md`
</output>
