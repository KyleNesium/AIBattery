---
phase: 10-popover-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - AIBattery/Views/Components/GaugeBar.swift
  - AIBattery/Views/UsageBarsSection.swift
  - AIBattery/Views/TokenHealthSection.swift
  - AIBatteryCoreTests/Views/GaugeBarTests.swift
autonomous: true
requirements: [PERF-10, PERF-11, PERF-12]

must_haves:
  truths:
    - "Popover opens instantly with no perceptible lag"
    - "TimelineViews only tick while popover is in view hierarchy (SwiftUI handles this automatically)"
    - "GeometryReader count is reduced from 4 to 3 unique call sites (shared GaugeBar replaces 2 duplicates)"
  artifacts:
    - path: "AIBattery/Views/Components/GaugeBar.swift"
      provides: "Reusable gauge bar component with single GeometryReader"
      contains: "struct GaugeBar"
    - path: "AIBatteryCoreTests/Views/GaugeBarTests.swift"
      provides: "Unit tests for GaugeBar"
      contains: "GaugeBarTests"
  key_links:
    - from: "AIBattery/Views/UsageBarsSection.swift"
      to: "AIBattery/Views/Components/GaugeBar.swift"
      via: "GaugeBar(percent:barColor:)"
      pattern: "GaugeBar\\("
    - from: "AIBattery/Views/TokenHealthSection.swift"
      to: "AIBattery/Views/Components/GaugeBar.swift"
      via: "GaugeBar(percent:barColor:)"
      pattern: "GaugeBar\\("
---

<objective>
Consolidate the duplicate GeometryReader progress bar pattern into a shared GaugeBar component, reducing layout pass overhead on popover open. Verify all previously-applied fixes (v1.9.4) satisfy PERF-10 and PERF-11.

Purpose: PERF-12 asks to reduce GeometryReader count. Two identical GeometryReader progress bar patterns exist in UsageBarsSection and TokenHealthSection. Extracting a shared GaugeBar view reduces unique GeometryReader call sites from 4 to 3 and eliminates code duplication. PERF-10 and PERF-11 are already satisfied by the v1.9.4 fixes committed to main.

Output: New GaugeBar component, updated sections, tests, build passes.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-popover-performance/10-CONTEXT.md

<interfaces>
<!-- Both sections use identical GeometryReader progress bar pattern -->

From AIBattery/Views/UsageBarsSection.swift (lines 81-93):
```swift
GeometryReader { geometry in
    ZStack(alignment: .leading) {
        RoundedRectangle(cornerRadius: Layout.barCornerRadius)
            .fill(ThemeColors.trackFill)
            .frame(height: Layout.barHeight)
        RoundedRectangle(cornerRadius: Layout.barCornerRadius)
            .fill(ThemeColors.barColor(percent: percent))
            .frame(width: geometry.size.width * min(CGFloat(percent) / 100.0, 1.0), height: Layout.barHeight)
    }
}
.frame(height: Layout.barHeight)
```

From AIBattery/Views/TokenHealthSection.swift (lines 69-81):
```swift
GeometryReader { geometry in
    ZStack(alignment: .leading) {
        RoundedRectangle(cornerRadius: Layout.barCornerRadius)
            .fill(ThemeColors.trackFill)
            .frame(height: Layout.barHeight)
        RoundedRectangle(cornerRadius: Layout.barCornerRadius)
            .fill(bandColor)
            .frame(width: geometry.size.width * min(CGFloat(health.usagePercentage) / 100.0, 1.0), height: Layout.barHeight)
    }
}
.frame(height: Layout.barHeight)
```

From AIBattery/Views/Components/ (existing shared components directory pattern):
- ThemeColors, Layout, Typography are in shared constants
- Layout.barCornerRadius and Layout.barHeight already defined
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract GaugeBar component from duplicate GeometryReader patterns</name>
  <files>AIBattery/Views/Components/GaugeBar.swift, AIBattery/Views/UsageBarsSection.swift, AIBattery/Views/TokenHealthSection.swift, AIBatteryCoreTests/Views/GaugeBarTests.swift</files>
  <read_first>
    - AIBattery/Views/UsageBarsSection.swift (lines 78-96 — GeometryReader bar)
    - AIBattery/Views/TokenHealthSection.swift (lines 68-84 — GeometryReader bar)
    - AIBattery/Views/Components/ (ls — see existing component patterns)
    - AIBattery/Models/ThemeColors.swift (Layout.barHeight, Layout.barCornerRadius, ThemeColors.trackFill)
  </read_first>
  <behavior>
    - GaugeBar(percent: 50, barColor: .blue) renders a bar filling 50% of available width
    - GaugeBar(percent: 0, barColor: .green) renders only the track (zero-width fill)
    - GaugeBar(percent: 150, barColor: .red) clamps fill to 100% width (no overflow)
    - GaugeBar uses Layout.barHeight for frame height and Layout.barCornerRadius for rounding
    - GaugeBar uses ThemeColors.trackFill for the background track
    - UsageBarsSection uses GaugeBar instead of inline GeometryReader
    - TokenHealthSection uses GaugeBar instead of inline GeometryReader
  </behavior>
  <action>
    1. Create AIBattery/Views/Components/GaugeBar.swift with a `struct GaugeBar: View` that takes `percent: Double` and `barColor: Color`. Internally uses one GeometryReader to measure width, renders the ZStack with track + fill bar. Uses Layout.barHeight, Layout.barCornerRadius, ThemeColors.trackFill. Clamp percent to 0...100 range.

    2. Write tests in AIBatteryCoreTests/Views/GaugeBarTests.swift verifying:
       - GaugeBar body builds without crash for percent values 0, 50, 100, 150, -10
       - Confirm clamping logic: percent values outside 0-100 are clamped

    3. Replace the GeometryReader block in UsageBarsSection.swift (lines 81-93) with:
       `GaugeBar(percent: percent, barColor: ThemeColors.barColor(percent: percent))`
       Keep the .accessibilityElement modifier chain after it.

    4. Replace the GeometryReader block in TokenHealthSection.swift (lines 69-81) with:
       `GaugeBar(percent: health.usagePercentage, barColor: bandColor)`
       Keep the .accessibilityElement modifier chain after it.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift build 2>&1 | tail -5 && swift test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "struct GaugeBar" AIBattery/Views/Components/GaugeBar.swift
    - grep -q "GaugeBar(" AIBattery/Views/UsageBarsSection.swift
    - grep -q "GaugeBar(" AIBattery/Views/TokenHealthSection.swift
    - grep -c "GeometryReader" AIBattery/Views/UsageBarsSection.swift returns 0
    - grep -c "GeometryReader" AIBattery/Views/TokenHealthSection.swift returns 0
    - swift build succeeds
    - swift test passes
  </acceptance_criteria>
  <done>GaugeBar component exists, both sections use it, no inline GeometryReader remains in UsageBarsSection or TokenHealthSection, all tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Verify PERF-10/11 fixes and audit for remaining lag sources</name>
  <files>AIBattery/Views/StatusBarManager.swift, AIBattery/Views/UsageBarsSection.swift, AIBattery/Views/PopoverFooterView.swift</files>
  <read_first>
    - AIBattery/Views/StatusBarManager.swift (full file — panel open/close path, isPanelShowing gating)
    - AIBattery/Views/UsageBarsSection.swift (TimelineView usage around line 98)
    - AIBattery/Views/PopoverFooterView.swift (TimelineView usage around line 159)
    - AIBattery/Models/MotionConstants.swift (animation durations)
  </read_first>
  <action>
    Audit and verify — no code changes expected unless issues found:

    1. PERF-10 verification: Confirm frame resize observer is debounced (16ms) and gated on isPanelShowing in StatusBarManager. Confirm NSApp.activate is called after makeKeyAndOrderFront. Confirm MotionConstants uses 0.15s/0.1s easeOut.

    2. PERF-11 verification: Confirm TimelineView in UsageBarsSection and PopoverFooterView are standard SwiftUI TimelineViews (not Timer.publish). SwiftUI automatically stops ticking when views leave the hierarchy on panel close. Confirm no Timer.publish exists anywhere in the view layer.

    3. Final GeometryReader audit: After Task 1, confirm remaining GeometryReaders are:
       - MarqueeText:48 (container width — required, already optimized with PreferenceKey for text measurement)
       - MarqueeText:62 (text width via PreferenceKey overlay — lightweight, no layout cost)
       - InsightsRowsAndHover:86 (Charts API requirement — cannot be eliminated)
       - GaugeBar.swift (consolidated from 2 sites into 1 reusable component)

    4. If any Timer.publish or unnecessary GeometryReader is found, remove it. Otherwise, this task is verification-only.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && echo "Timer.publish count:" && grep -rc "Timer\.publish" --include="*.swift" AIBattery/ || echo "0" && echo "GeometryReader sites:" && grep -rc "GeometryReader" --include="*.swift" AIBattery/Views/ | grep -v ":0" && swift build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Timer.publish" across AIBattery/Views/ returns 0
    - grep "isPanelShowing" AIBattery/Views/StatusBarManager.swift finds gating logic
    - grep "debounce\|DispatchWorkItem" AIBattery/Views/StatusBarManager.swift finds frame resize debounce
    - Total GeometryReader count across AIBattery/Views/ is 4 (MarqueeText x2, InsightsRowsAndHover x1, GaugeBar x1)
    - swift build succeeds
  </acceptance_criteria>
  <done>All v1.9.4 fixes verified in place. No Timer.publish in view layer. GeometryReader count reduced from 4 inline sites to 3 unique sites (1 reusable). PERF-10, PERF-11, PERF-12 all satisfied.</done>
</task>

</tasks>

<verification>
1. `swift build` succeeds with no warnings related to changed files
2. `swift test` passes — all existing + new GaugeBar tests green
3. `grep -rc "GeometryReader" AIBattery/Views/ | grep -v ":0"` shows 4 total (GaugeBar:1, MarqueeText:2, InsightsRowsAndHover:1)
4. `grep -rc "Timer.publish" AIBattery/Views/` shows 0
5. No inline GeometryReader in UsageBarsSection or TokenHealthSection
</verification>

<success_criteria>
- GaugeBar shared component exists and is used by both sections
- GeometryReader duplication eliminated (2 identical patterns consolidated into 1 reusable)
- Zero Timer.publish in view layer (SwiftUI TimelineView used instead)
- Frame resize observer debounced and gated on panel visibility
- All tests pass including new GaugeBar tests
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/10-popover-performance/10-01-SUMMARY.md`
</output>
