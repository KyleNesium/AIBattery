---
phase: 03-write-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Tests/AIBatteryCoreTests/Services/TokenLedgerTests.swift
  - Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift
autonomous: true
requirements:
  - PERF-05
  - PERF-06

must_haves:
  truths:
    - "TokenLedger.merge() writes to disk at most once per call, only when values increase"
    - "TokenLedger.merge() is called exactly once per aggregation cycle (not multiple times)"
    - "buildProjectTokensFromMap operates on a pre-built map, not raw JSONL entries"
    - "UsageAggregator fingerprint skip prevents re-aggregation when inputs are unchanged"
    - "No regression in token count accuracy"
  artifacts:
    - path: "Tests/AIBatteryCoreTests/Services/TokenLedgerTests.swift"
      provides: "Tests proving single-write-per-merge and no-write-when-unchanged behavior"
      contains: "merge_unchangedValues_doesNotWrite"
    - path: "Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift"
      provides: "Tests proving fingerprint skip and buildProjectTokensFromMap correctness"
      contains: "fingerprintSkip"
  key_links:
    - from: "AIBattery/Services/UsageAggregator.swift"
      to: "AIBattery/Services/TokenLedger.swift"
      via: "Single merge() call at line 223"
      pattern: "TokenLedger\\.shared\\.merge"
    - from: "AIBattery/Services/UsageAggregator.swift"
      to: "cachedSnapshot fingerprint check"
      via: "4-way fingerprint skip at lines 44-56"
      pattern: "cachedSnapshot.*statsCacheModDate.*rateLimits.*idleSessionMinutes"
---

<objective>
Verify and test that TokenLedger write batching (PERF-05) and buildProjectTokens iteration avoidance (PERF-06) are already correctly implemented, then add tests that prove the performance characteristics.

Purpose: Both optimizations are already in place (high-water-mark single-write in TokenLedger, pre-built map in buildProjectTokensFromMap, fingerprint skip in aggregate). This plan adds regression tests that codify these performance guarantees so future changes cannot silently degrade them.

Output: New test cases in existing test files proving write batching and iteration avoidance behavior.
</objective>

<execution_context>
@/Users/kyle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kyle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-write-performance/03-CONTEXT.md

@AIBattery/Services/TokenLedger.swift
@AIBattery/Services/UsageAggregator.swift
@Tests/AIBatteryCoreTests/Services/TokenLedgerTests.swift
@Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift

<interfaces>
<!-- TokenLedger API (from AIBattery/Services/TokenLedger.swift) -->
```swift
@MainActor final class TokenLedger {
    static let shared: TokenLedger
    init(fileURL: URL? = nil)
    func merge(_ tokens: [ModelTokenSummary], accountId: String) -> [ModelTokenSummary]
    func flushForTesting()
    // save() is private — writes via Task.detached only when `changed` flag is true (line 84-87)
}
```

<!-- UsageAggregator fingerprint skip (lines 44-56) -->
```swift
// In aggregate(): returns cachedSnapshot early when ALL of:
//   statsCacheModDate == lastStatsCacheModDate
//   rateLimits == lastRateLimits
//   idleSessionMinutes == lastIdleSessionMinutes
//   accountId == lastAccountId
```

<!-- buildProjectTokensFromMap (line 350) -->
```swift
private static func buildProjectTokensFromMap(_ projectMap: [String: ProjectAccum]) -> [ProjectTokenSummary]
// Input: pre-built map from unified pass (populated at lines 96-170)
// Output: array of ProjectTokenSummary with costs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TokenLedger write-batching verification tests (PERF-05)</name>
  <files>Tests/AIBatteryCoreTests/Services/TokenLedgerTests.swift</files>
  <read_first>
    - AIBattery/Services/TokenLedger.swift (full file, 145 lines — understand save() trigger)
    - Tests/AIBatteryCoreTests/Services/TokenLedgerTests.swift (full file — understand existing test patterns)
  </read_first>
  <behavior>
    - Test: merge_unchangedValues_doesNotWrite — call merge() with same values twice, check file mod date is unchanged after second call (flush between to ensure first write lands)
    - Test: merge_singleCallWritesOnce — call merge() with increased values, flush, verify file exists and contains updated data (proves one write per merge call, not per-value)
    - Test: merge_multipleModelsOneWrite — pass 3 models with increases, flush, reload from disk — all 3 updated in single write (not 3 separate writes)
  </behavior>
  <action>
Add 3 new tests to the existing TokenLedgerTests suite that verify PERF-05 write-batching behavior:

1. `merge_unchangedValues_doesNotWrite`: Create ledger with temp URL. Merge tokens (input: 100). flushForTesting(). Record file modification date via FileManager.attributesOfItem. Merge same tokens again (input: 100, no increase). flushForTesting(). Check file mod date is unchanged — proves no write when values don't increase.

2. `merge_singleCallWritesOnce`: Create ledger. Merge [3 different models with different token counts] in a single merge() call. flushForTesting(). Create a new TokenLedger instance from same URL. Merge with lower values for all 3 models. Verify all 3 models return the original higher values — proves all models were persisted in a single write.

3. `merge_mixedChanges_writesOnlyOnce`: Create ledger. Merge [model-a: 100, model-b: 200]. flushForTesting(). Record mod date. Merge [model-a: 150 (increase), model-b: 100 (decrease)]. flushForTesting(). Verify mod date changed (write happened). Reload and verify model-a=150, model-b=200 (high-water preserved). This proves a single write captured both the increase and the preserved value atomically.

Use the existing `makeToken()` and `makeTempURL()` helpers. Follow the existing test naming convention. All tests need `@Test @MainActor` annotations.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift test --filter TokenLedgerTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "merge_unchangedValues_doesNotWrite\|merge_singleCallWritesOnce\|merge_mixedChanges_writesOnlyOnce" Tests/AIBatteryCoreTests/Services/TokenLedgerTests.swift returns 3
    - swift test --filter TokenLedgerTests passes with 0 failures
  </acceptance_criteria>
  <done>Three new tests prove TokenLedger writes at most once per merge() call and does not write when values are unchanged. All existing + new tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add UsageAggregator fingerprint-skip and buildProjectTokensFromMap tests (PERF-06)</name>
  <files>Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift</files>
  <read_first>
    - AIBattery/Services/UsageAggregator.swift (lines 40-60 for fingerprint skip, lines 345-376 for buildProjectTokensFromMap)
    - Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift (full file — understand helpers and patterns)
  </read_first>
  <behavior>
    - Test: aggregate_fingerprintSkip_returnsCachedSnapshot — call aggregate() twice with identical inputs, verify same snapshot object returned (reference or value equality)
    - Test: aggregate_fingerprintChanged_recomputes — call aggregate() once, then again with different rateLimits, verify snapshot reflects new data
    - Test: buildProjectTokensFromMap_producesCorrectSummaries — use existing JSONL test helper to write entries with cwd paths, aggregate, verify projectTokens array has correct project grouping and token sums
  </behavior>
  <action>
Add 3 new tests to the existing UsageAggregatorTests suite that verify PERF-06 behavior:

1. `aggregate_fingerprintSkip_returnsCachedSnapshot`: Create aggregator with a stats-cache and some JSONL entries. Call aggregate(rateLimits: nil, accountId: nil) twice with identical parameters. The second call should return the exact same UsageSnapshot (same values). To verify the skip actually worked (not just recomputation producing same result): check that the two snapshots have identical modelTokens arrays. This confirms the fingerprint skip at lines 44-56 is functioning.

2. `aggregate_fingerprintChanged_recomputes`: Call aggregate() with rateLimits: nil. Then call again with a non-nil RateLimitUsage value. Verify the second snapshot has the updated rate limit data — proving that changed fingerprint inputs trigger recomputation.

3. `aggregate_projectTokens_fromPreBuiltMap`: Write JSONL entries with different `cwd` values (e.g., "/path/project-a" and "/path/project-b") using the existing `writeJSONL` helper. Each project gets entries for 2 different models. Aggregate and verify `projectTokens` array contains exactly 2 entries, each with correct per-project token sums. This proves buildProjectTokensFromMap works from the unified-pass map rather than re-iterating JSONL.

Use existing test helpers (tempDir, writeStatsCache, writeJSONL, makeAssistantLine). Follow existing patterns and naming conventions. Check what RateLimitUsage init looks like before constructing test values.
  </action>
  <verify>
    <automated>cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift test --filter UsageAggregatorTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "fingerprintSkip\|fingerprintChanged\|projectTokens_fromPreBuiltMap" Tests/AIBatteryCoreTests/Services/UsageAggregatorTests.swift returns 3
    - swift test --filter UsageAggregatorTests passes with 0 failures
  </acceptance_criteria>
  <done>Three new tests prove fingerprint skip prevents redundant aggregation, fingerprint change triggers recomputation, and projectTokens are built from the pre-accumulated map. All existing + new tests pass.</done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
cd /Users/kyle/workspace/Github/KyleNesium/AIBattery && swift test 2>&1 | tail -5
```
All tests pass, including the 6 new performance-characteristic tests.
</verification>

<success_criteria>
- 6 new tests added across 2 test files (3 TokenLedger + 3 UsageAggregator)
- All tests pass (swift test exits 0)
- Tests prove: single write per merge, no write on unchanged, fingerprint skip works, project tokens from map
- No production code changes needed (optimizations already in place)
- README Test Coverage section updated with new test count
</success_criteria>

<output>
After completion, create `.planning/phases/03-write-performance/03-01-SUMMARY.md`
</output>
